# Stav rozpracované práce – web Raška engineering

_Poslední aktualizace: 2026-09-03_

## Mobilní úpravy – toto sezení

Otestováno přes Playwright (Chromium, mobilní viewport 390×844 + kontrola 320/375/430 px na horizontální přetečení – žádné nenalezeno).

**Opravená chyba:** Po otevření mobilního menu (`.mobile-nav`) zmizelo logo i zavírací (×) tlačítko v hlavičce a tlačítko nešlo ani kliknout (`.mobile-nav` ho vizuálně i interakčně překrývalo), přestože `.site-header` má `z-index: 100` a `.mobile-nav` jen `z-index: 99`. Příčina: `.nav-shell` (obsahuje logo i tlačítko) neměl nastavenou `position`, takže je podle pravidel CSS stackování prohlížeč vykresloval jako nepozicovaný prvek vždy POD jakýmkoli pozicovaným sourozencem (zde `.mobile-nav`) – bez ohledu na z-index rodiče. Oprava: `.nav-shell` dostal `position: relative; z-index: 101;` (`style.css`, sekce 5 Hlavička). Ověřeno screenshoty i funkčním kliknutím na zavírací tlačítko bez potřeby force-kliku; desktop header beze změny.

**Vyřešeno:** `.feature-grid` (karty O nás – Komplexní řešení / Bezpečnost / atd.) přepnuta na mobilu z 2 na 1 sloupec (base `grid-template-columns: 1fr`, beze změny zůstává `@media (min-width:640px) → repeat(4,1fr)`). Desktop (4 sloupce) ověřen beze změny.

**Vyřešeno – přestavba mobilního hera:** Uživatel nahlásil, že na mobilu byl hero text částečně nečitelný a tlačítka ležela přes fotku. Nové řešení pro mobil (`max-width:767px`, desktop nedotčen):
- Vznikl nový obrázek `Obrazky/web/hero-photos-mobile.jpg` (1200×714) – ořez PRAVÉ (fotografické) části aktuální `hero-collage-desktop.jpg`, tj. jen elektro/fotovoltaika/průmysl panely BEZ modré plochy. Modro/elektro šev nalezen přímým vzorkováním pixelů (ne odhadem): solid modrá `(21,58,103)` končí na `x≈1138` při `y=0` (slope `-0.13` sedí s již známým vzorcem švů), takže ořez `(1140, 0, 2400, 750)` z desktop koláže → resize na 1200×714 (dochoval poměr stran, žádné další oříznutí bokem). Vybrán horní pás (y 0–750 z 2000) obsahující výrazné detaily (červené kontrolky jističů, tlakoměr) – porovnáno vizuálně s pásy ze středu/spodu.
- HTML (`index.html`): `.hero-inner` (text) přesunut PŘED `.hero-media` v DOM (na desktopu nehraje roli, `.hero-media` tam zůstává `position:absolute` pozadí; na mobilu teď určuje pořadí v běžném toku). `<img>` na mobilu teď ukazuje na `hero-photos-mobile.jpg`, `width/height` upraveny na 1200×714.
- CSS (`style.css`, sekce 6 Hero): na mobilu `.hero-media` už není `position:absolute` full-bleed pozadí, ale běžný blok `position:relative; aspect-ratio:1200/714;` hned pod textem – modrá plocha za textem je jen samotný gradient `.hero` (stejná formule jako `.site-footer`). `.hero-inner` na mobilu má zrušenou velkou `min-height` rezervu (`min-height:auto`) a menší `padding-bottom`, protože už neslouží k naskládání prostoru pod fotkou na pozadí. `object-position` na `.hero-media img` sjednocen na `center center` pro obě šířky (zrušen zvláštní mobilní `center top`, redundantní `@media(min-width:768px)` blok smazán).
- Staré `Obrazky/web/hero-collage-mobile.jpg` (1200×2200, s modrou nahoře) zůstává v souborech nepoužité, pro případný návrat.
- Ověřeno screenshoty na 390px, 320px (žádné horizontální přetečení) i na desktopu 1440px (hero i feature-grid beze změny).

**Doladění po zpětné vazbě uživatelky (eyebrow "nalepený" na menu, fotka moc zakrytá kartou):**
- `.hero-content` na mobilu dostal `margin-top: calc(var(--header-height) + var(--space-8))` (přidán odstup eyebrow od hlavičky).
- `.hero-content h1` na mobilu zmenšen na `clamp(2rem, 1.5rem + 4vw, 2.5rem)`, `.hero-content .lead` na `font-size: 1rem` (desktop beze změny, oboje jen v `@media (max-width:767px)`).
- `hero-photos-mobile.jpg` přegenerován na PLNOU výšku zdrojového pruhu (ořez `(1140,0,2400,2000)` místo jen `(1140,0,2400,750)`), tj. 1200×1905 místo 1200×714 – uživatelka chtěla mobilní fotku vyšší, "klidně stejně vysokou jako pro desktop, ten výřez", protože při krátké verzi kartu služby (`.services-overlap`, přesah `-8.6rem`) zakrývala téměř celou fotku. `.hero-media` `aspect-ratio` v CSS upraven na `1200/1905`, `<img>` width/height v HTML na `1200/1905`.

**Třetí kolo doladění (padding hero, foto o něco nižší, karta pohotovosti vyšší):**
- `.hero-actions` (tlačítka) na mobilu blíž textu: `margin-top` snížen z `--space-12` na `--space-6`. **Pozor, tahle první verze se ve skutečnosti vůbec neprojevila** – pravidlo bylo omylem zapsané do `@media(max-width:767px)` bloku, který se v souboru nachází PŘED obecným (nepodmíněným) pravidlem `.hero-actions { margin-top: var(--space-12); }` – při stejné specificitě vyhrává pozdější pravidlo v pořadí zdroje, takže mobil dál používal 48px. Opraveno až ve čtvrtém kole (viz níže) přesunutím override AŽ ZA obecné pravidlo.
- Padding modré hero plochy na mobilu sjednocen nahoře/dole: `.hero-inner` teď má `padding-top: calc(var(--header-height) + var(--space-8))` a `padding-bottom: var(--space-8)` (dřív byl nesymetrický – navíc ještě `.hero-content` vynucovalo `min-height: calc(100vh - header-height)`, což dělalo mezeru dole mnohem větší než nahoře). `.hero-content` na mobilu teď má `margin-top:0; min-height:auto` – veškerý odstup od hlavičky řeší jen `.hero-inner`'s padding-top.
- `hero-photos-mobile.jpg` znovu přegenerován, mírně nižší: ořez `(1140,0,2400,1600)` místo `(1140,0,2400,2000)` → 1200×1524 (místo 1200×1905). `.hero-media` `aspect-ratio` a `<img>` width/height upraveny na `1200/1524`.
- Karta „Potřebujete elektrikáře rychle?" (`.band-inner`) na mobilu zvýšena (`min-height: 32rem`) a maska `.band-photo` posunuta tak, aby bylo dole jasně vidět víc fotky (`mask-image` mobilní varianta: transparentní do 40 %, plně krycí už od 82 % místo 52 %/100 %) – text nahoře zůstává stejně velký/čitelný, dole navíc vznikl prostor, kterým fotka zřetelně prosvítá.
- Vše ověřeno na 390px i desktopu 1440px (beze změny na desktopu).

**Čtvrté kolo (uživatelka nahlásila, že tlačítka jsou "pořád daleko od textu"):** Skutečná příčina byla ta CSS pořadová chyba popsaná výše – `margin-top` na mobilu byl ve skutečnosti pořád 48px, ne 24px. Opraveno: mobilní override `.hero-actions { margin-top: var(--space-4); }` (16px) přesunut do nového `@media(max-width:767px)` bloku HNED ZA obecné pravidlo `.hero-actions` (`style.css` cca řádek 644). Ověřeno přímým výpočtem `getComputedStyle` na 390px (mezera lead→tlačítka teď 32px celkem, dřív 64px) i screenshotem; desktop beze změny (pořád 48px).

**Poučení pro příště:** u jakéhokoli mobilního `@media(max-width:...)` override je nutné ho v souboru fyzicky umístit AŽ ZA odpovídající obecné (nepodmíněné) pravidlo se stejným selektorem/specificitou – jinak ho to pozdější obecné pravidlo v cascade přebije a mobilní úprava se navenek vůbec neprojeví, i když vypadá v kódu správně. Při jakékoli podobné úpravě raději rovnou ověřit `getComputedStyle` na daném viewportu, ne jen věřit zápisu v CSS.

## Páté kolo úprav (další sada mobilních požadavků)

- **Fotka v pásu "Potřebujete elektrikáře rychle?"** – na mobilu (úzká/vysoká karta, `min-height:32rem`) `background-position: center 34%` ořezávalo hlavně obličej/ruku, multimetr a rozvodná skříň (vpravo ve zdrojové fotce `pohotovost-band.jpg`, 1800×649) byly mimo záběr. Přidán mobilní override `background-position: 0% 0%, 80% 34%;` (dvě hodnoty = gradient vrstva beze změny, fotka posunuta doprava). Ověřeno screenshotem – teď jasně vidět multimetr i rozvaděč.
- **Sekce O nás – prohozené pořadí text/fotka na mobilu.** V `index.html` přesunut `.about-content` (text) PŘED `.about-media` (fotka) a `.about-watermark` v DOM. Na desktopu (1024px+) je `.about-media` `position:absolute` (kryje se jako pozadí za textem vpravo), takže pořadí v DOM tam nehraje roli – ověřeno screenshotem, beze změny. Na mobilu (normální tok, žádné absolutní pozicování) teď jde: text → fotka → hned navazující glass karty (`.feature-grid`, přesah `margin-top:-3.5rem` teď kouše do spodku FOTKY místo textu). Vedlejší efekt: `.about-watermark` (translucentní gear logo, `position:absolute;bottom:3.5rem` vůči celému `.about-split`) by se po prohození ocitl schovaný v fotce místo u paty textu – opraveno mobilním override `bottom: calc(75vw + 3.5rem)` (75vw = přesná výška `.about-media` na mobilu, protože má `aspect-ratio:4/3` na plnou šířku bez containeru).
- **Naše realizace – diagonální řez i na mobilu + menší ikony.** Mobilní mřížka `.showcase-gallery` (3 sloupce × 2 řady, `gap:0`) dostala stejný princip šikmého řezu jako desktopová flex verze, jen s menším úhlem (`--showcase-skew-m: 0.85rem` místo desktopových `2rem`) a přes `:nth-child(3n+1/+2/3n)` (první/prostřední/poslední fotka v KAŽDÉ ze 2 řad, místo `:first-child`/`:last-child` jako na desktopu, kde je jen 1 řada). `.showcase-tag` (kroužek s ikonou oboru) na mobilu zmenšen z 3.75rem na 2.25rem (ikona uvnitř z 2.1rem na 1.25rem), odsazení z `--space-4` na `--space-2` – dřív zabíral skoro polovinu šířky malé fotky. Diagonála je na malém near náhledu jemná, ale při přiblížení jasně vidět a fungovala i vizuálně v plném screenshotu.
- **CTA pás "Chcete se dozvědět více o našich službách?"** – padding na mobilu zmenšen z `padding-top:6rem / padding-bottom:7rem` na `padding-top/bottom: var(--space-12)` (48px/48px). Ověřeno `getComputedStyle`.
- **Patička – navigace ve 2 sloupcích po 3 (do 1023px).** `.footer-nav-col ul` dostal do `@media(max-width:1023px)` `display:grid; grid-template-columns:repeat(2,1fr)` s explicitním `grid-column`/`grid-row` po `:nth-child` (HTML pořadí položek beze změny – Domů, Elektro služby, Fotovoltaika, Průmyslová řešení, O nás, Kontakt): levý sloupec = služby (Elektro služby, Fotovoltaika, Průmyslová řešení), pravý sloupec = Domů, O nás, Kontakt. Od 1024px zůstává původní jednosloupcové zarovnání na výšku (`flex; justify-content:space-between`) beze změny.

Všech 5 bodů ověřeno screenshoty na 390px i plným průchodem desktopu (1440px) – beze změny na žádné z upravovaných sekcí.

## Šesté kolo úprav

- **Fotka v pásu pohotovosti – posun na "vrchní část multimetru".** Zjištěno, že `background-position` Y na mobilu neměl vůbec žádný efekt: box (331.5×512) a zdrojová fotka (1800×649) mají tak odlišný poměr stran, že "cover" se vždy přesně srovná na VÝŠKU (512=512×1.024) a ořízne jen šířku – žádná svislá rezerva k posunu neexistuje, ať je Y jakékoliv. Řešení: nový samostatný soubor `Obrazky/web/pohotovost-band-mobile.jpg` (750×500), oříznutý přímo z originálu (`(1050,20,1800,520)`) tak, aby ukazoval hlavně ciferník/tělo multimetru + rozvaděč a co nejméně zápěstí/ruky. Použit jen na mobilu přes `background-image`/`background-position` override (ne celý `background` shorthand, aby zůstalo zachováno `background-size:cover` a gradient overlay z základního pravidla). Doladěná pozice `21% center`. Displej multimetru (LCD) kvůli fixní 40% skryté horní zóně masky není 100 % v záběru, ale ciferník (rotační přepínač) i rozvaděč ano – podstatně méně "jen ruka" než původně. Pokud by uživatelka chtěla i displej výrazněji, je potřeba předělat zdrojový ořez tak, aby displej ležel níž v novém obrázku (blíž viditelné spodní zóně masky), případně mírně posunout práh masky – zatím neřešeno, čeká na zpětnou vazbu.
- **Logo (vodoznak) v O nás blíž kraji na mobilu.** `.about-watermark` mobilní override doplněn o `right: 0` (dřív dědilo `right:4%` ze základního pravidla, stejné jako desktop).
- **Bílý pruh dole u fotek na šířku v mobilní galerii – opravená chyba.** Skutečná příčina: `margin-left` (záporný, pro překryv diagonálního řezu) na CSS Grid itemu se `align-items:stretch` (default) donutí prohlížeč dopočítat VĚTŠÍ šířku daného itemu (track šířka mínus záporný margin = víc místa), a protože `.showcase-photo` má `aspect-ratio:3/4` odvozený z vlastní šířky, položky s záporným marginem (prostřední/poslední ve řádku) vyšly širší A VYŠŠÍ než první položka v řádku – řádek se pak roztáhl na výšku těch vyšších, a first-in-row položka (bez marginu, tedy nižší) nechala dole mezeru = bílý pruh. Netýkalo se to konkrétní orientace fotky, ale POZICE ve řádku (first-in-row), což se jen náhodou překrývalo s tím, že landscape fotky byly zrovna v řádku 2. Oprava: `margin-left` nahrazen `transform: translateX(...)` – transform je čistě vizuální posun, vůbec neovlivňuje layout/šířku, takže všechny položky mají teď stejnou šířku (130px) i výšku (173px), žádná mezera. Ověřeno přímým měřením přes `getComputedStyle`/`getBoundingClientRect`.
- **Standardní mobilní padding sekcí (`--section-padding-mobile: var(--space-12)`, 48px).** Nový CSS custom property v `:root`. Aplikován na `.section` (jen `padding-top`, aby zůstalo zachováno `.showcase-section`ovo záměrné `padding-bottom:0`), `.about-section` a `.contact-section` (oba `padding-block`) – vše v novém bloku na úplném konci `style.css` (sekce 15), aby při stejné specificitě spolehlivě přebil všechna dřívější nepodmíněná pravidla pro tytéž sekce. `.showcase-cta-band` (nastaveno minulé kolo) přepsáno, ať používá stejnou proměnnou místo natvrdo `--space-12`. Desktop hodnoty (64px/96px/96px/96+112px) ověřeny beze změny.

Všechny 4 body ověřeny na 390px i desktopu 1440px.

## Sedmé kolo úprav

- **Logo (vodoznak) v O nás – zmenšeno a odsazení sjednoceno s pravým okrajem.** Nejdřív zkoušeno doslova `bottom:0; right:0` (roh fotky) podle zadání "stejný odstup jako od pravého okraje". Zjištěno (ověřeno screenshotem se zvýrazněným kontrastem), že logo s `opacity:0.2` položené přímo na busy fotku je prakticky neviditelné – na desktopu je vidět jen proto, že tam sedí na ploché modré ploše, ne na fotce. Po konzultaci s uživatelkou zůstává řešení: logo zůstává na modré ploše (nad fotkou, stejně jako desktop), `bottom: 75vw` (= přesná výška `.about-media`, tj. logo těsně nad horní hranou fotky, nulová mezera) + `right: 0`, `width` zmenšena z `14rem` na `6rem`, poté na základě zpětné vazby (bylo příliš malé) zvětšena na `11rem`. Odstup od spodní (horní hrana fotky) i pravé hrany modré plochy nakonec sjednocen na `var(--space-2)` (0.5rem) místo nulového, ať logo trochu "dýchá" v prostoru. Ověřeno screenshotem, desktop (`bottom:3.5rem; right:4%; width:14rem`) beze změny.
- **Fotka v pásu pohotovosti – zkusili jsme užší ořez (750×350) ukazující displej multimetru, ale uživatelce se nelíbil (ztratil se rozvaděč, moc přiblížené) → vráceno zpět.** `pohotovost-band-mobile.jpg` je znovu 750×500 (dial + rozvaděč, stejně jako předtím), jen ořez z originálu posunut z `(1050,20,1800,520)` na `(1050,0,1800,500)` – tj. využito zbylých 20px prostoru nad multimetrem v originálu (výš už zdrojová fotka nejde, 0 je její úplný horní okraj). `background-position-x` zpět na `21%`. **Důležité omezení zjištěné v tomto kole:** displej multimetru (LCD) je ve zdrojové fotce jen ~170–195px od úplného horního okraje (y=0), zatímco maska `.band-photo` na mobilu odkrývá fotku až od 40 % výšky karty dolů – při zachování širšího záběru (dial+rozvaděč, výška ořezu ~500px) se displej nikdy nedostane výš než ~40–44 % masky, tedy zůstane v zóně, kde je téměř neviditelný. Displej lze plně odkrýt jen užším/vyšším ořezem (viz zavržený pokus výše), který ale oříznutím rozvaděče mění charakter fotky. Pokud by uživatelka chtěla displej vidět A zachovat širší záběr zároveň, řešením by bylo upravit práh masky (`mask-image` na `.band-photo` v mobilním bloku) tak, aby odkrývala fotku od nižšího procenta (např. 25 % místo 40 %) – zatím neprovedeno, čeká na rozhodnutí, protože to může zhoršit čitelnost textu nad fotkou.
- **Fotka v pásu pohotovosti – nahrazena vlastní fotkou s dostatkem prostoru nad multimetrem.** Uživatelka doplnila do `Obrazky/Pohotovost/pohotovost-4.jpg` novou (vlastní, vysoké rozlišení 5472×3648) fotku téhož multimetru, tentokrát s mnohem větším odstupem nad přístrojem (technik, helma, rozvaděč nahoře) – na rozdíl od dřívějšího úzkého stock-banneru (`pohotovost-band.jpg`, 1800×649) tu konečně byl prostor obojí zároveň: displej i širší záběr s rozvaděčem. Nový ořez `(2350,1845,4000,2945)` → 1650×1100, zmenšeno na 900×600 pro `Obrazky/web/pohotovost-band-mobile.jpg`. Displej (v ořezu y≈550–800 z 1100, tj. ~50–73 % výšky) padne do viditelné/dofukované zóny masky (40–82 %), `background-position-x` doladěn na `23%`. Výsledek: displej multimetru i ciferník viditelné zároveň, plus rozmazaný (mimo ostrost fotografa) kousek rozvaděče vpravo. Ověřeno screenshotem, desktop (jiný soubor/pravidlo, `pohotovost-band.jpg` + `center 34%`) beze změny.
- **Fotka v pásu pohotovosti – oddáleno, ať jsou multimetr i rozvaděče vidět bez detailního přiblížení.** Nový, mnohem širší ořez ze stejné fotky (`pohotovost-4.jpg`): `(1984,100,5472,3400)` → 3488×3300 (skoro čtvercový, ne úzký panoramatický pás jako předtím), zmenšeno na 1200×1135 pro `pohotovost-band-mobile.jpg`. Ukazuje multimetr, celý bílý rozvaděč s jističi a kus tyrkysové rozvodné skříně vpravo – žádný detail, spíš celkový záběr. `background-position-x` přepočítán na `52%` (menší přesah do stran než u předchozích užších ořezů, protože tento crop má aspect ratio blíž boxu, cover má míň vodorovné rezervy k posunu). Ověřeno screenshotem, desktop beze změny.
- **Fotka v pásu pohotovosti – rozvodná skříň odstraněna ze záběru.** Uživatelka chtěla jen ruku, multimetr a jističe, bez tyrkysové rozvodné skříně. Ořez zúžen z `(1984,100,5472,3400)` na `(1984,100,4250,3400)` (2266×3300 místo 3488×3300 – jen pravá hranice, skříň začínala až za x=4250), zmenšeno na 850×1238. `background-position-x` zpět na `50%` (aspect ořezu 0.687 je teď velmi blízko boxu 0.6475, takže cover má jen ~20px vodorovné rezervy, prakticky bez potřeby posunu). Ověřeno screenshotem, desktop beze změny.
- **Fotogalerie – vrácen rovný (nešikmý) řez na mobilu.** Diagonální řez z minulého kola (transform + clip-path na `:nth-child(3n±)`) způsoboval nový bílý pruh v BOKU některých fotek – transform posouval box jen vizuálně, ale sousední clip-path polygony se přesně nekryly s posunutou hranou, takže v místě švu problikávalo pozadí. Uživatelka požádala o návrat ke standardnímu rovnému oddělení. Řešení: celý blok `transform`/`clip-path` pro `.showcase-item:nth-child(...)` na mobilu smazán – zůstává jen základní `.showcase-gallery { grid-template-columns: repeat(3,1fr); gap:0 }`, což dává čistou mřížku 3×2 bez mezer i švů. Zmenšené odznaky ikon (`.showcase-tag` 2.25rem) z minulého kola zůstávají beze změny. Ověřeno `getBoundingClientRect` – všech 6 fotek má identickou velikost 130×173.3px, `clip-path:none`, `transform:none`. Desktop (šikmý řez, `--showcase-skew:2rem`) beze změny.

## O webu

Statická marketingová prezentace firmy Raška engineering s.r.o. (elektroinstalace, fotovoltaika, průmysl). Žádný build systém – čisté `index.html` + `style.css` + `script.js`, obrázky ve složce `Obrazky/`. Testuje se přímo přes `file://`.

## Designový systém – konvence pro novou stránku (co není zřejmé z běžného zadání)

Než se navrhne nová stránka/sekce v duchu stávajícího designu, tohle je potřeba dodržet – běžné zadání ("modrá firma, oranžové CTA") tyhle detaily nepokryje:

1. **Tři responsive vrstvy, ne dvě.** Web se nedělá jen pro "mobil + desktop", ale pro tři pásma se skutečně odlišným layoutem: mobil `≤767px`, tablet `768–1023px`, desktop `≥1024px` (viz `--header-height`, sekce 15/16 v `style.css`). Navíc existuje jedna účelová výjimka: hero má ještě mezikrok `1024–1399px` ("menší notebooky"), protože `.container` (85 %, max 1280px) na těchto šířkách nechává jinou mezeru než na širokých monitorech. Drobné doladění jsou i na `480px` (`.nav-phone-cta` se objeví) a `640px` (grid sloupce). Nová sekce by měla od začátku počítat s vlastním chováním na všech třech hlavních pásmech, ne se spoléhat, že tablet "to zdědí" z desktopu – přesně tohle byl kořen skoro všech tabletových oprav v tomhle sezení.
2. **Standardní vertikální padding sekce** (viz "Pravidla pro budoucí práci" výše): mobil `var(--section-padding-mobile)`=48px, tablet `var(--space-16)`=64px, desktop laděno individuálně (48–112px podle důrazu sekce). Nová sekce má tohle dodržet, ne si vymýšlet vlastní hodnotu.
3. **Container / full-bleed trik.** `.container` = 85 % šířky, max 1280px, `margin-inline:auto`. Několik prvků ale záměrně "prokoukne" mimo container na celou šířku obrazovky (`left:50%; width:100vw; transform:translateX(-50%)` nebo přímo `width:100vw; margin-left:calc(50% - 50vw)`) – používá se u `.showcase-gallery` (galerie realizací) a u gradientového pozadí hero textu na tabletu (`.hero-inner::before`). Kdykoli je vidět "pruh přes celou šířku obrazovky uvnitř jinak vycentrované stránky", jde o tenhle vzor, ne o vlastní layout.
4. **`--tunnel-inset` pro fotku-vlevo/text-vpravo layouty.** Sekce "O nás" (`.about-content`) počítá svůj boční padding jako `calc((100% - min(var(--container-width), var(--container-max))) / 2)` – tj. i když sekce sama je full-bleed (bez `.container`), text uvnitř zůstává zarovnaný na stejnou neviditelnou osu jako `.container` obsah nad/pod ní. Používat všude, kde má full-bleed sekce obsahovat text zarovnaný s containerem.
5. **Karty jsou "glass" (sklo), ne plná bílá.** Skleněné karty (`.service-card`, `.feature-item`, `.contact-form-card`, hlavička/mobilní menu) mají všechny stejnou recepturu: `background: rgba(255,255,255,0.5)`, `backdrop-filter: blur(28px) saturate(160%)` (u hlavičky jen `blur(10–20px)` bez saturate), `border: 0.5px solid rgba(255,255,255,0.12)`, `box-shadow: var(--shadow-lg), inset 0 1px 0 rgba(255,255,255,0.18)`. Nová karta v tomhle stylu musí použít stejnou kombinaci, ne jen `background:white`.
6. **Karty přesahující přes hranu předchozí sekce.** `.services-overlap` má záporný `margin-top` (mění se podle šířky, teď navíc tabletová výjimka `-6rem` jen na peek přes zkrácenou hero fotku) – to je záměrný "přesah" vzor, používaný jen mezi hero a kartami služeb. Nekopírovat mechanicky jinam bez rozmyslu, kolik má karta "vykukovat".
7. **Diagonální řez (`clip-path: polygon(...)`) se používá na dvou místech** – `.about-media` (fotka O nás na desktopu) a `.showcase-gallery` (galerie realizací od 768px). Na mobilu je diagonála buď vypnutá (O nás – nemá na mobilu žádný `clip-path`, jde o normální obdélník), nebo zmenšená na jemnější úhel (`--showcase-skew-m` u galerie) – nikdy stejná hodnota skewu jako na desktopu, protože na úzké fotce by uřízla příliš.
8. **Dva odlišné gradienty pro dvě barevné role**, nezaměňovat: modrý "brand" gradient sekcí/pozadí je `linear-gradient(160deg, primary-dark 0%, primary-dark 55%, primary 100%)` (hero, případně `115deg`/`165deg` varianty u `.band`/mobilního menu – směr úhlu se mezi komponentami mírně liší, ale vždy primary-dark→primary). Oranžový CTA gradient je vždy `135deg, accent 0%, accent-dark 100%` (ikony, `.btn-accent` používá `90deg` variantu). Nekombinovat směry/barvy nahodile.
9. **Tlačítka: `border: 2px solid transparent` na base `.btn`, i tam, kde tlačítko žádný viditelný rámeček nemá.** Díky tomu můžou varianty jako `.btn-outline-dark` na hoveru nastavit `border-color: transparent` a nechat gradientovou výplň vyplnit i prostor pod rámečkem beze švu (viz "Sekundární tlačítka" výše) – nová varianta tlačítka s barevným rámečkem + hover výplní musí tenhle vzor dodržet, jinak vznikne viditelný lem.
10. **Ikony: dva systémy vedle sebe.** UI ikony (šipka, telefon, menu, zavřít, sociální sítě) jsou inline SVG sprite (`<symbol id="icon-...">` v `<svg style="display:none">` na začátku `<body>`, použití přes `<use href="#icon-x">`). Fotografické/produktové ikony (elektro, fotovoltaika, průmysl, feature-item ikony) jsou samostatné PNG soubory v `Obrazky/web/`. Nová ikona v UI kontextu (tlačítko, nav) patří do SVG sprite; nová "kategorie/obor" ikona do PNG po vzoru stávajících.
11. **Scroll-reveal animace přes `data-reveal`** – atribut na kontejneru (ne na jednotlivých dětech), `IntersectionObserver` v `script.js` přidává `.is-visible`, CSS definuje jen počáteční/cílový stav (`[data-reveal]` → `[data-reveal].is-visible`). Vždy respektovat `@media (prefers-reduced-motion: reduce)` (animace úplně vypnutá). Nová sekce, která má "najet" při scrollu, přidá jen atribut `data-reveal` na svůj wrapper, ne vlastní animaci.
12. **Fixní hlavička vyžaduje offset všude, kde se na sekci skáče kotvou.** `--header-height` (5.0625rem/81px) se přičítá k `padding-top` (hero) nebo k `scroll-margin-top` (kotvy typu `#sluzby`). Nová sekce s vlastní kotvou (menu odkaz, "Zjistit více" apod.) musí mít `scroll-margin-top: calc(var(--header-height) + odstup)`, jinak jí po scrollu bude horní část schovaná pod menu – přesně tenhle bug se řešil u tlačítka "Naše služby".
13. **Fotky pro různé breakpointy jsou často samostatné, předem oříznuté soubory, ne jen CSS `object-position`.** Hero a pohotovostní pás mají zvlášť `*-mobile.jpg`/`*-desktop.jpg` verze (přesné pixelové ořezy dohledané vzorkováním barev, ne odhadem) – `object-fit:cover` s jedním obrázkem přes všechny šířky často nestačí (viz "Hero fotokoláž" a "Rozměry hero pozadí" výše). Nová fotografická sekce, kde se motiv na úzkých šířkách hodně ořeze, by měla rovnou počítat s vlastním mobilním/tabletovým výřezem, ne to řešit jen posunem `object-position`.

## Pravidla pro budoucí práci

- **Vertikální padding sekcí musí zůstat sladěný napříč breakpointy.** Při přidání/úpravě jakékoli obsahové sekce vždy zkontrolovat `padding-top`/`padding-bottom` (`getComputedStyle`) na desktopu, tabletu (768–1023px) i mobilu (≤767px) a porovnat s ostatními sekcemi na stejné šířce – ne se spoléhat na to, že `rem`-based hodnota napsaná pro desktop bude na tabletu vypadat konzistentně (viz "Sjednocení vertikálního paddingu sekcí na tabletu" níže, kde se to bez kontroly rozjelo na 48–112px). Aktuální standard: mobil `var(--section-padding-mobile)` (48px, sekce 15 v `style.css`), tablet `var(--space-16)` (64px, sekce 16), desktop dle sekce (48–112px, laděno individuálně). Výjimky (`.services-overlap` horní padding 0, `.showcase-section` dolní padding 0) jsou záměrné – u nové sekce podobnou výjimku zdůvodnit komentářem, ne nechat jako náhodu.

## Struktura stránky (`index.html`)

1. **Hlavička** – sticky header, mobilní menu
2. **Hero** (`.hero`) – fotokoláž na pozadí + úvodní text
3. **Karty služeb** (`#sluzby`, `.services-overlap`) – přesahují přes ostrou hranu hero sekce do bílého pozadí (elektro / fotovoltaika / průmysl)
4. **Pohotovostní elektro služby** (`.band`)
5. **O nás** (`.about-section`)
6. **Naše realizace** (`.showcase-section`) – galerie s lightboxem
7. **CTA pás před kontaktem** (`.showcase-cta-band`)
8. **Kontakt** (`#poptavka`) – formulář s honeypot antispamem
9. **Patička**
10. **Cookie lišta** (Přijmout / Odmítnout / Nastavit, ukládá se do `localStorage`)
11. **Lightbox galerie realizací**

`script.js` řeší: sticky header, mobilní menu, scroll-reveal animace (`data-reveal` + IntersectionObserver), cookie lištu, lightbox galerie, honeypot kontrolu formuláře.

## Hero fotokoláž – hlavní rozpracovaná věc tohoto sezení

Hero pozadí NENÍ tvořeno CSS clip-path panely, ale dvěma hotovými (zploštělými) JPG obrázky bez editovatelných vrstev:

- `Obrazky/web/hero-collage-desktop.jpg` (2400×2000)
- `Obrazky/web/hero-collage-mobile.jpg` (1200×2200)

Každý zobrazuje 4-panelovou diagonální koláž: plná modrá (brand) → elektro closeup → fotovoltaika → průmysl.

### Aktuální (finální) stav desktop koláže

- **Elektro panel** – beze změny, původní.
- **Fotovoltaika panel** – ořez `(1600, 820, 2280, 2680)` ze zdroje `Obrazky/kolaz/aerial-view-private-house-with-solar-panels-roof.jpg`, resize na 725×2000. Iterativně doladěno na max. počet panelů, bez oken/vikýře, s malým (uživatelem odsouhlaseným) kouskem tašek v levém dolním rohu. **Uživatel potvrdil: "fotovoltaika je teď super."**
- **Průmysl panel** – ořez `(374, 100, 1282, 3104)` ze zdroje `Obrazky/kolaz/closeup-view-cylindrical-grinder-industrial-concept.jpg` (potrubí/ventily), resize na 595×2000. Mezitím se zkoušela i nová fotka `Obrazky/kolaz/prumysl.jpg` (extrudovací linka s oranžovým násypkou), ale uživatel se rozhodl **vrátit zpět na fotku s potrubím** – to je aktuální stav. `prumysl.jpg` zůstává ve složce pro případné budoucí použití, momentálně se nepoužívá.
- **Stínování švů** – na obě diagonální hrany (elektro/fotovoltaika a fotovoltaika/průmysl) je aplikován jemný multiplikativní gradient (numpy), simulující vržený stín, konzistentní s přirozeným stínem na původním švu panel1/panel2.
- **Mobilní koláž byla v tomto sezení nahrazena** – viz sekce "Mobilní úpravy – toto sezení" výše: nový `hero-photos-mobile.jpg` (jen 3 fotopanely, bez modré, 1200×714) nahradil starý `hero-collage-mobile.jpg` (1200×2200, s modrou nahoře) coby zdroj mobilního `<img>` v hero sekci. Starý soubor zůstává v `Obrazky/web/` nepoužitý.

### Poznámka k souborům

Ve složce `Obrazky/web/` existují záložní kopie `hero-collage-desktop (kopie).jpg` a `hero-collage-mobile (kopie).jpg` – jde o zálohy před poslední úpravou, lze je smazat, až bude finální stav definitivně odsouhlasen.

### Geometrie koláže (pro budoucí úpravy)

Desktop: jeden konzistentní sklon švu `x(y) = x0 − 0.13·y` napříč celou koláží (ověřeno na čistém švu modrá/elektro). Panel průmysl má pravou hranu rovnou (okraj obrázku), levou diagonální podle vzorce. Přesné vzorce švů použité v tomto sezení:
- `seam23(y) = 1600 - 0.13*y` (elektro/fotovoltaika)
- `seam34(y) = 2065 - 0.13*y` (fotovoltaika/průmysl)

Mobil: fotopás začíná vodorovnou hranou na y=1320, dolů k y=2200; švy diagonální se stejným principem.

Podrobný postup (crop → mask → paste → shadow gradient) je uložen v projektové paměti (`project_hero_collage.md`), včetně důležitého pravidla: při přebarvování jednoho panelu je nutné vždy čerstvě překreslit OBA sousední panely v jednom průchodu a teprve poté aplikovat stín přes oba švy – jinak hrozí zdvojení stínu na již hotovém sousedním panelu.

## Rozměry hero pozadí (informační zjištění z tohoto sezení)

Desktop šířka koláže 2400 px je v pořádku; minimální potřebná výška byla dopočítána ze CSS formulí `.hero` / `.hero-media` / `.hero-inner` (min-height `100vh + Nrem` dle breakpointu) tak, aby obrázek pokryl i nejvyšší běžné monitory bez roztažení.

## Tabletové úpravy – toto sezení

**Vyřešeno – přesah hero fotky přes tlačítko telefonu:** Uživatelka požádala o doladění hero sekce na tabletu. Diagnostika (Playwright na šířkách 768–1440px + pixelové vzorkování) odhalila, že obrysové tlačítko `+420 733 686 898` (`.hero-actions`) leží velkou částí přes fotku jističů z koláže (`.hero-media`), s velmi špatným kontrastem bílého textu na světlém plastu jističe – **problém sahal od 768px až do ~1400px**, tedy nejen tablety, ale i běžné notebooky (1280×800, 1366×768).

Příčina: sloupec s textem (`.hero-content`, `max-width: clamp(29rem, 20vw, 32rem)` – v tomto rozsahu šířek se `clamp` prakticky vždy ustálí na pevných 29rem, protože `20vw` je menší) má šířku prakticky nezávislou na okně, zatímco `.hero-media` je vždy nalepená na PRAVÝ okraj celé obrazovky (`right:0; width:100%`) – velký "gutter" mezi textem a fotkou vzniká teprve když `.container` (85% šířky, max 1280px) přestane růst 1:1 s oknem, což je až kolem ~1400–1440px.

Oprava (`style.css`, sekce 6 Hero, nový blok `@media (min-width:768px) and (max-width:1399px)`): `.hero-media` dostala pevný `left: 34rem` (544px) místo `width:100%` (šířka teď `auto`, `right:0` zůstává) – levý okraj koláže je tak vždy dost daleko napravo, aby nezasahoval pod tlačítka, a šířka viditelné fotky přirozeně roste s šířkou okna. Ověřeno screenshoty i výpočtem (`mediaLeft` vs. `phoneRight`) na 768/820/900/1024/1200/1366/1399px (vše čisté) i na hranicích 1400/1440px a mobilu 767px (beze změny oproti původnímu stavu).

**Otevřené:** stejný přesah (do ~1400px) technicky existuje i na běžných noteboocích – uživatelka žádala jen o tablet, ale oprava řeší oba případy najednou (jde o stejné pravidlo/rozsah).

**Přepracováno na základě zpětné vazby – zúžená koláž ukazovala jen elektro panel:** Uživatelka nahlásila, že po předchozí opravě je na tabletu vidět jen tenký svislý pruh elektro panelu a zbytek koláže (fotovoltaika, průmysl) je skrytý – chtěla stejný princip jako na mobilu (text přes celou modrou plochu, koláž jako pruh POD textem).

Řešení rozdělené na dva rozsahy podle toho, co se u obou reálně osvědčilo:
- **768–1023px (tablet, `style.css` nový blok na konci sekce 6 Hero):** `.hero-content{max-width:none}` (text se roztáhne přes celou šířku), `.hero-inner` dostal stejnou strukturu jako mobil (`width:100%`, zrušená velká `min-height` rezerva, padding nahoře/dole), `.hero-media` přestala být `position:absolute` full-bleed pozadí a stala se normálním blokem (`position:relative; width:100%; aspect-ratio:12/5;`) v běžném toku hned pod textem – širší (méně vysoký) poměr stran než mobilní `1200/1524`, protože tablet má na šířku mnohem víc místa. Žádný nový obrázek nebyl potřeba – použita stejná `hero-collage-desktop.jpg` (přes `<picture>` `srcset` pro `min-width:768px`), `object-fit:cover` s výchozím `object-position:center center` ukazuje vodorovný pruh přes celou koláž (elektro + fotovoltaika + část průmyslu).
- **1024–1399px (menší notebooky):** ponechána oprava z předchozího kola (`.hero-media{left:34rem;width:auto}`, koláž zůstává vedle textu jako na desktopu) – na těchto šířkách bylo vedle sebe vidět dost fotky (celá diagonála, všechny 3 panely), takže tam přestavba na "mobilní" pruh nebyla potřeba ani žádoucí.

Ověřeno screenshoty na 768, 820, 1023px (nový skládaný layout, koláž viditelná celá) i na 1024px landscape a 1440px desktop (obojí beze změny oproti předchozímu stavu).

**Opraveny dvě chyby nalezené uživatelkou hned po nasazení výše:**
1. **Nadpis byl schovaný pod fixní hlavičkou.** Příčina: `padding-top`/`padding-bottom` v novém tabletovém bloku používaly `var(--space-10))` – token `--space-10` v tomto projektu vůbec neexistuje (škála skáče `space-8`→`space-12`), takže celý neplatný `calc()` spadl na `0px` (potvrzeno `getComputedStyle` – `padding-top` byl skutečně `0px` místo očekávaných ~129px). Oprava: nahrazeno existujícím `var(--space-12)` na všech třech místech (`.hero-inner` padding-top/-bottom, `.hero-media` margin-top).
2. **V pruhu koláže byla vidět i modrá plocha.** Příčina: `<picture>` v `index.html` měl jediný zdroj `<source media="(min-width:768px)" srcset="hero-collage-desktop.jpg">` – tablet tedy dostával STEJNÝ obrázek jako desktop (celou koláž včetně modrého panelu 1), a protože nový box je širší (`aspect-ratio:12/5`) než tento obrázek (2400×2000), `object-fit:cover` ořezával jen svisle a modrý panel zůstal vždy vidět vlevo. Oprava: přidán mezistupeň zdroje – `<source media="(min-width:1024px)" srcset="hero-collage-desktop.jpg">` (celá koláž jen od desktopu) + `<source media="(min-width:768px)" srcset="hero-photos-mobile.jpg">` (768–1023px dostává STEJNÝ, už jednou oříznutý obrázek bez modré, co používá mobil) – zdroje se vyhodnocují shora dolů, takže na 1024px+ vyhraje první (celá koláž), na 768–1023px druhý (bez modré), pod 768px se použije `<img>` fallback (týž soubor).

Obojí ověřeno přes `getComputedStyle`/`currentSrc` i screenshoty na 768/820/900/1023px (nadpis celý pod hlavičkou, žádná modrá v pruhu) a zpětně na 767/1024/1440px (beze změny).

## Mobilní menu – telefonní tlačítko

**Vyřešeno:** V rozklikávacím mobilním menu (`#mobilni-menu`) byl telefonní kontakt jen prostý odkaz s ikonou (`.phone-link.phone-link--mobile`), zatímco uživatelka chtěla stejné oranžové tlačítko jako v hlavičce desktopu (`.nav-phone-cta`, `.btn.btn-accent`).

- `index.html`: odkaz přepsán z `<a class="phone-link phone-link--mobile">` na `<a class="btn btn-accent btn-block">` (stejné třídy jako tlačítko "Nezávazná poptávka" pod ním).
- `style.css`: nepoužívané třídy `.phone-link` a `.phone-link--mobile` (byly jen na tomto jednom místě) smazány.
- **Nalezená a opravená druhá chyba:** po prostém přidání tříd `.btn` se tlačítko zobrazilo rozbité (ikona nad textem na dvou řádcích, bez paddingu) – příčina: `.mobile-nav a { display:block; font-size:1.25rem; padding: var(--space-4) 0; border-bottom: ...}` má vyšší specificitu (třída+element) než `.btn` (jen třída), takže přebilo flex zarovnání i padding tlačítka. Oprava: nové pravidlo `.mobile-nav a.btn` (vyšší specificita než obě) vrací `display:inline-flex; align-items:center; justify-content:center`, padding shodný s `.btn-accent` (`padding-block/inline: calc(0.7rem/1.4rem + 2px)`), `font-size:1rem`, `border-bottom:none`, plus `margin-bottom: var(--space-4)` pro odstup od tlačítka "Nezávazná poptávka" pod ním.

Ověřeno screenshoty na 320/375/430/767px (žádné horizontální přetečení, tlačítko vycentrované, stejný styl jako "Nezávazná poptávka") i na desktopu 1440px (hlavičkové `.nav-phone-cta` beze změny).

**Doplněno:** tlačítko "Nezávazná poptávka" v mobilním menu na žádost uživatelky odstraněno (`index.html`) – v menu teď zůstává jen telefonní tlačítko. `.mobile-nav a.btn { margin-bottom: var(--space-4) }` zůstává (odsazení od spodního paddingu menu), CSS beze změny.

## Patička – barva odkazu harmonickyweb.cz

**Vyřešeno:** Odkaz na tvůrce webu (`.footer-bottom a`) byl `var(--color-accent)` (oranžová), uživatelka chtěla jen jemně světlejší šedou než copyright text vedle něj (ten má `rgba(255,255,255,0.55)`). Změněno na `rgba(255,255,255,0.8)`. Platí pro všechny šířky (žádný media query se sem netrefuje).

## Scroll na tlačítko "Naše služby" (hero) – dojíždělo pod fixní hlavičku

**Vyřešeno:** Tlačítko `href="#sluzby"` v hero sekci scrollovalo tak, že vršek `#sluzby` (=.services-overlap, ikony první řady karet) přistál přesně na `y=0`, tedy schovaný pod fixní hlavičkou (81px / `--header-height: 5.0625rem`) – ikony a horní část karet byly useknuté. `#sluzby` nemělo žádný `scroll-margin-top`.

- **Fix:** `.services-overlap { scroll-margin-top: calc(var(--header-height) + var(--space-6)); }` – prohlížeč teď při scrollu na kotvu nechá pod hlavičkou ještě `--space-6` rezervu, takže je vidět celá první karta včetně ikony.
- Ověřeno Playwright kliknutím na tlačítko na 1440/768/390px šířky – `sectionRect.top` po scrollu ~105px (81px header + 24px rezerva) na všech třech, screenshoty potvrzují kompletní kartu i ikonu pod menu bez oříznutí.

## Sekundární tlačítka (.btn-outline-dark) – rámeček viditelný přes gradient na hoveru

**Vyřešeno:** U `.btn-outline-dark` ("Zjistit více" v kartách služeb) byl na hoveru rámeček pořád plnou barvou `var(--color-primary)`, zatímco výplň se měnila na gradient `var(--color-primary) → var(--color-primary-dark)` – na pravé straně tlačítka (kde gradient ztmavne) byl rámeček oproti výplni rozeznatelný.

- **Fix:** `.btn-outline-dark:hover { border-color: transparent; }` – base `.btn` má `border: 2px solid transparent` a výchozí `background-clip: border-box`, takže s průhledným rámečkem gradient vyplní i prostor pod ním a švík zmizí.
- Ověřeno Playwright screenshotem hoveru – rámeček splývá s výplní, normální (nehover) stav beze změny. `.btn-outline` (varianta na tmavém pozadí, používaná jinde) stejný problém nemá, tam se barva rámečku na hoveru sladěně mění spolu s výplní.

## Tablet hero – druhé kolo doladění (paddingy, gradient, rozestupy, výška fotky, přesah karty)

Uživatelka nahlásila pět věcí na tabletovém (768–1023px) hero po předchozí přestavbě na "mobilní" skládaný layout:

1. **Jiné paddingy kolem textu.** `.hero-inner` měl pevný `padding-inline: var(--space-8)` (32px) místo běžného chování `.container` (85% šířky, max 1280px) – dávalo to textu o ~26px víc místa než mají karty služeb pod ním (`.services-overlap .container`), takže levý/pravý okraj textu s ničím pod sebou nelícoval (na 768px: text od x=32, karty od x=57.6). **Fix:** `width/max-width/padding-inline` přebití z `.hero-inner` u tabletu odstraněno – necháno na běžném `.container`. Aby to nerozbilo `.hero-content .eyebrow { white-space:nowrap }` (vynucené na jeden řádek pro úzký desktopový sloupec vedle koláže, zděděné i do tabletu), přidáno `white-space: normal` – bez postranního sloupce už nehrozí zalomení uprostřed slova a chová se to stejně jako na mobilu (kde `nowrap` nikdy neplatilo).
2. **Gradient z hero pozadí nebyl vidět.** `.hero` má `linear-gradient(160deg, primary-dark 0%, primary-dark 55%, primary 100%)`, ale na tabletu `.hero-media` není `position:absolute` (jako na desktopu), nýbrž běžný blok v toku – prodlužuje tak celkovou výšku `.hero` o výšku fotky. Světlejší konec gradientu (55–100 %) proto vycházel na oblast schovanou za neprůhlednou fotkou a nad textem zůstávala jen plochá tmavá barva. **Fix:** stejný gradient přesunut na `.hero-inner::before` – `position:absolute; top:0; bottom:0` (= přesně výška textové zóny) + `left:50%; width:100vw; transform:translateX(-50%)` (trik na "prokouknutí" z 85%-container šířky do celé obrazovky, jinak by na okraji `.hero-inner` vznikl viditelný šev proti ploché barvě `.hero` za ním – ověřeno a opraveno, první verze bez tohoto tricku měla viditelný obdélníkový obrys).
3. **Velká mezera mezi textem a tlačítky / nesladěné rozestupy.** Eyebrow→nadpis→text→tlačítka měly 24/32/64px (nekonzistentní, `.hero-actions margin-top: var(--space-12)=48px` navíc ve flexu NEkoluje s `.lead margin-bottom`, ale sčítá se – `.hero-content` je `flex-direction:column`). **Fix:** `.hero-content h1{margin-bottom:var(--space-6)}`, `.hero-content .lead{margin-bottom:0}`, `.hero-actions{margin-top:var(--space-6)}` – všechny tři mezery teď rovných 24px.
4. **Hero fotky moc nízké.** `.hero-media{aspect-ratio:12/5}` (2.4, na šířku) na zdrojovém `hero-photos-mobile.jpg` (1200×1905, poměr 0.787) ukazoval přes `object-fit:cover` jen prostřední ~33 % výšky obrázku – silně "seříznutý" pohled na všechny tři panely (elektro/FV/průmysl). **Fix:** `aspect-ratio: 4/3` – ukáže se ~60 % výšky, panely jsou čitelnější.
5. **Karty mají jen "kouknout".** `.services-overlap` dědil `margin-top:-9.6rem` (153.6px) ze společného pravidla pro 768px+ – v kombinaci s vyšší fotkou (bod 4) by to znamenalo ještě větší zásah do fotky. **Fix:** tabletu vlastní `margin-top: -3rem` (48px) – jen zaoblený vršek karty s ikonou vykoukne přes spodní okraj fotky, zbytek karty je až v běžném bílém toku pod ní.

Ověřeno Playwright screenshoty na 768 a 1023px šířky (paddingy lícují s kartami, gradient plynulý bez švu, rozestupy 24/24/24px, fotka ~576–767px vysoká, karta kouká jen ~48px) a zpětně na mobilu (390px) a desktopu (1024/1366/1440px) – všude beze změny oproti předchozímu stavu.

**Třetí kolo (na základě screenshotu od uživatelky):** po nasazení výše se objevily tři nové připomínky.

- **Viditelná modrá čára na rozhraní hero/fotka.** Mezera před fotkou byla rozdělená na dvě části – `.hero-inner` `padding-bottom: var(--space-12)` (kryté gradientem z `::before`) + samostatný `.hero-media margin-top: var(--space-12)` (mimo `.hero-inner`, kryté jen plochou barvou `.hero`) – na jejich rozhraní vznikl ostrý přechod ze světlé (konec gradientu) do tmavé (plochá barva). **Fix:** celá mezera (96px) přesunuta do `.hero-inner{padding-bottom: var(--space-24)}`, `.hero-media` už nemá vlastní `margin-top` – všechno je teď v jedné, gradientem kryté ploše, žádný švík.
- **Karty nevylézaly.** `-3rem` přesah byl tak malý (48px), že přes vyšší fotku (bod 4 výše) působil jako by karta vůbec nevyčuhovala. **Fix:** `.services-overlap margin-top: -6rem` (96px) – zřetelný, ale ne přehnaný peek (ověřeno screenshotem na 768 i 1023px – vidět celá ikona a začátek nadpisu karty nad bílou plochou).
- **Text moc nahustonahuštěný.** Sjednocené rozestupy z předchozího kola (24px/`--space-6`) byly na vkus uživatelky příliš těsné. **Fix:** zvětšeno na 32px/`--space-8` u všech čtyř mezer (eyebrow mb, h1 mb, lead mb→actions mt) – pořád jednotné, jen vzdušnější.

Ověřeno znovu na 768/1023px (bez švu, karta zřetelně kouká, mezery 32/32/32px) a zpětně na mobilu a desktopu (beze změny).

**Čtvrté kolo:** dva další požadavky.

- **Nesouměrný padding nahoře/dole kolem hero textu.** Viditelná mezera nad eyebrow (`padding-top` minus `--header-height`, tedy jen `--space-12`=48px) neodpovídala mezeře pod tlačítky před fotkou (`padding-bottom: var(--space-24)`=96px, zavedeno ve 3. kole kvůli švu). **Fix:** `padding-bottom` zpátky na `var(--space-12)` (48px) – teď stejné jako nahoře. Mezera před fotkou se tím zmenšila ze 96 na 48px, ale zůstává celá uvnitř `.hero-inner` (kryté gradientem), takže se švík z 3. kola nevrátil.
- **Karty pořád nevylézaly dost.** I s `-6rem` přesahem (96px) to na vyšší fotce (aspect-ratio 4/3, 576–767px) vypadalo jako málo – 96px je vůči takové výšce jen 12–17 %. **Fix (podle instrukce uživatelky "zkrátit koláž"):** `aspect-ratio` zmenšen ze 4/3 na `3/2` (fotka 512–682px vysoká, pořád ukazuje ~52 % výšky zdrojové koláže místo původních ~33 % u 12/5) – při stejném 96px přesahu je karta vůči nižší fotce vizuálně mnohem víc "venku".

Ověřeno na 768 a 1023px (padding nahoře/dole shodný, karta zřetelně přesahuje přes kratší fotku) a zpětně na mobilu a desktopu (beze změny).

**Páté kolo:** uživatelce se ten menší (48px) padding nelíbil o nic víc než nesouměrný – chtěla radši ten původní větší, ale na obou stranách stejně. **Fix:** `padding-top: calc(--header-height + var(--space-24))`, `padding-bottom: var(--space-24)` – oboje 96px viditelně (nahoře k tomu navíc `--header-height` pro fixní hlavičku). Mezera před fotkou je pořád celá uvnitř `.hero-inner` (žádný švík), přesah karty (96px) i poměr fotky (3/2) beze změny. Ověřeno na 768/1023px a zpětně na mobilu/desktopu (beze změny).

**Šesté kolo:** poslední připomínka – ve výřezu koláže (aspect-ratio 3/2, `object-position` dosud výchozí `center center`) bylo vidět moc z vrchu obrázku (horní část elektro rozvaděče) a málo ze středu (fotovoltaika). **Fix:** `.hero-media img { object-position: center 65%; }` (jen v tabletovém bloku, stejná specificita/pořadí jako ostatní přebití) – posune viditelné okno níž ve zdrojovém obrázku, takže je víc vidět fotovoltaika a průmysl, míň horní okraj rozvaděče. Netýká se mobilu (tam `object-position` nemá efekt, box = poměr zdroje 1:1) ani desktopu (jiný obrázek `hero-collage-desktop.jpg`, jiný box). Ověřeno na 768/1023px a zpětně na mobilu/desktopu (beze změny).

## Karta pohotovosti (.band) na tabletu – přeuspořádáno jako na mobilu

Obecné pravidlo `@media(min-width:768px){.band-inner{flex-direction:row;...}}` platilo od 768px výš, takže tablet zdědil ŘÁDKOVÉ uspořádání z desktopu (ikona/text/tlačítko vedle sebe, diagonální maska na širokoúhlé fotce `pohotovost-band.jpg` 1800×649) – na tabletové šířce to bylo přehuštěné: text se lámal do mnoha úzkých řádků, tlačítko i fotka byly stěsnané doprostřed karty (viz `band-t768.png`/`band-t1023.png` před fixem v historii konverzace).

**Fix:** nový blok `@media (min-width:768px) and (max-width:1023px)` vrací `.band-inner` na stejné SVISLÉ uspořádání jako na mobilu (`flex-direction:column; align-items:flex-start; gap:var(--space-6); padding:var(--space-8)`) a `.band-photo` na stejný svislý ořez/masku a mobilní crop `pohotovost-band-mobile.jpg` (850×1238) – širokoúhlá desktopová fotka by se na vysoké kartě přes `cover` ořízla jen vodorovně a svislá maska by nikdy nic neodkryla. Jediný rozdíl oproti mobilu: `min-height: 26rem` místo mobilních `32rem` – širší tabletový sloupec zalomí text do míň řádků, takže by stejná výška jako na mobilu zbytečně nechala prázdnotu nad fotkou.

Ověřeno screenshoty na 768/1023px (ikona/nadpis/text nahoře, tlačítko pod tím, fotka čitelná dole) a zpětně na mobilu (390px) a desktopu (1024/1440px) – beze změny, pořád řádkové uspořádání s diagonální maskou.

**Druhé kolo:** dvě doladění stejné karty na tabletu.

- **Multimetr nebyl vidět.** `.band-photo` `background-position` zděděné z mobilního fixu bylo `50% center` (svisle na střed) – tabletová karta je ale mnohem širší než mobilní sloupec, takže `cover` z fotky (850×1238) ukazuje jen ~44 % její výšky, a střed padá na řady jističů uprostřed obrázku, ne na multimetr v ruce dole. **Fix:** Y posunuto na `50% 85%` – stejná velikost/zoom (pořád `cover`, žádné přiblížení), jen jiné okno v tomtéž rámu, posunuté níž, takže je multimetr zase vidět (spolu s kouskem rozvaděče nad ním pro kontext).
- **Tlačítko doprava.** `.band .btn-pulse { align-self: flex-end; }` v tabletovém bloku – `.band-inner` je `flex-direction:column; align-items:flex-start`, tohle přebíjí zarovnání jen pro tlačítko, zbytek (ikona, nadpis, text) zůstává vlevo.

Ověřeno na 768/1023px (multimetr viditelný v kontextu rozvaděče, tlačítko vpravo dole) a zpětně na mobilu a desktopu (beze změny).

## Sekce "O nás" na tabletu – logo a karty jako na mobilu

Strukturně už `.about-split` na tabletu (768–1023px) BYLA stejná jako na mobilu (žádné pravidlo mezi mobilem a 1024px layout mění – text → fotka na celou šířku v běžném toku, `flex`/`clip-path` row layout je jen `min-width:1024px`). Dva konkrétní kusy ale chyběly:

- **Watermark logo Raška nebylo vidět.** `.about-watermark` mělo tabletu vlastní pravidlo jen na `max-width:767px` (`bottom: calc(75vw + var(--space-2))` — počítá s fotkou na celou šířku obrazovky, `aspect-ratio:4/3`). Na tabletu se místo toho použila výchozí (desktopová) hodnota `bottom:3.5rem`, počítaná pro `.about-split` s `min-height:36rem` z 1024px+ layoutu — na tabletu bez téhle výšky skončilo logo schované dole v samotné fotce, kde ho kvůli `opacity:0.2` nebylo přes fotku vůbec vidět. **Fix:** rozšířen mediální dotaz z `max-width:767px` na `max-width:1023px` – stejný vzorec (`75vw` = výška full-bleed fotky se `4/3` poměrem) platí na tabletu stejně dobře, protože `.about-media` je full-bleed (bez `.container`) na obou šířkách.
- **4 karty vedle sebe v jednom stěsnaném řádku.** `@media(min-width:640px){.feature-grid{grid-template-columns:repeat(4,1fr)}}` platilo od 640px výš (tablet i desktop) – na tabletu byly karty úzké, text se lámal do mnoha řádků. **Fix:** nové pravidlo `@media(min-width:768px) and (max-width:1023px){.feature-grid{grid-template-columns:repeat(2,1fr)}}` – karty po dvou pod sebou (2×2).

Ověřeno na 768/1023px (logo viditelné nad fotkou, karty 2×2) a zpětně na mobilu (1 sloupec, logo na svém místě) a desktopu 1024/1440px (4 karty vedle sebe, logo beze změny).

**Druhé kolo:** fotka (`.about-media`, aspect-ratio 4/3 na celou šířku obrazovky) byla na tabletu mnohem vyšší (576–767px podle šířky) než textový blok vedle "O nás" (`.about-content`, ~420px nezávisle na šířce) – uživatelka chtěla fotku nižší, jen o něco vyšší než text. **Fix:** `.about-media { aspect-ratio: auto; height: 28rem; }` (448px, pevná hodnota místo škálování se šířkou přes `aspect-ratio`) – místo `max-width:1023px` mediálního dotazu pro `.about-watermark` (zavedený v předchozím kole) znovu rozdělen zpátky na mobilní (`max-width:767px`, beze změny, pořád `75vw`-based formule pro fotku co škáluje se šířkou) a nový tabletový blok s `bottom: calc(28rem + var(--space-2))` – protože fotka už na tabletu neroste s `vw`, muselo se přepočítat i umístění loga nad ní.

Ověřeno na 768/1023px (fotka ~448px, jen mírně vyšší než text ~417–421px, logo pořád těsně nad fotkou) a zpětně na mobilu a desktopu (beze změny).

**Třetí kolo:** po zkrácení fotky (viz výše) bylo ve výřezu vidět zbytečně moc oblohy – sdílené pravidlo `.about-media img { object-position: left 35%; }` (platí na všech šířkách) bylo vychýlené směrem k vrchu fotky, a na nižším/širším tabletovém výřezu (`cover` ořízne jen málo svisle, protože box 768×448/1023×448 není o moc širší než poměr zdroje 4:3) to nechalo v okně hodně oblohy. **Fix:** `.about-media img { object-position: left 60%; }` jen v tabletovém bloku – posune okno níž ve zdroji (o kousek, ne na doraz), ať je víc auta/domu a míň oblohy; mobil a desktop mají jiný poměr fotky (jiné/žádné omezení výšky), takže tam sdílené `left 35%` zůstává beze změny.

Ověřeno na 768/1023px (méně oblohy, dům/auto výrazněji) a zpětně na mobilu a desktopu (beze změny).

## Galerie "Naše realizace" na tabletu – vyšší fotky

`@media(min-width:768px){.showcase-gallery{display:flex;...}}` platí od 768px výš (tablet i desktop) – všech 6 fotek vedle sebe v jednom diagonálně řezaném řádku, `.showcase-photo{aspect-ratio:3/4}`. Na desktopu (1440px) vychází sloupec ~240px široký → ~320px vysoký, v pořádku. Na tabletu je ale sloupec jen ~128–170px široký (768–1023px/6), takže při stejném poměru 3/4 vyšla fotka jen ~171–227px vysoká – nízký, "useknutý" pruh.

**Fix:** `@media(min-width:768px) and (max-width:1023px){.showcase-photo{aspect-ratio:3/6}}` – vyšší poměr jen pro tablet (počet sloupců i diagonální řez beze změny), fotky teď vychází ~256–341px vysoké.

Ověřeno na 768/1023px (výrazně vyšší, čitelnější fotky) a zpětně na mobilu (jiná pravidla, mřížka 3×2) a desktopu 1440px (beze změny, pořád aspect-ratio 3/4).

## Sjednocení vertikálního paddingu sekcí na tabletu

Uživatelka se ptala, jestli jsou paddingy sekcí napříč webem sladěné pro tablet (768–1023px) – v některých sekcích jí připadaly menší (hero záměrně mimo, viz zadání). Audit (`getComputedStyle` na `.services-overlap/.band/.about-section/.showcase-section/.showcase-cta-band/.contact-section`) potvrdil nesoulad – hodnoty jsou na tabletu identické s desktopem (žádné škálování v rámci 768–1023, jen `rem`-based): `.band` 48px, `.about-section`/`.showcase-section` 64px, `.contact-section` 96px, `.showcase-cta-band` 96/112px.

**Fix:** nový blok (sekce 16, na konci souboru vedle mobilního ekvivalentu ze sekce 15, kvůli cascade pořadí) sjednocuje `.band`, `.contact-section` a `.showcase-cta-band` na `var(--space-16)` (64px) jen pro 768–1023px – stejná hodnota, jakou už přirozeně mají `.section`/`.about-section`, takže žádná nová proměnná nebyla potřeba. `.services-overlap` (horní padding záměrně 0, karty přesahují přes hero) a `.showcase-section` (dolní padding záměrně 0, galerie navazuje na CTA pás bez mezery) ponechány beze změny – jde o úmyslný, ne nahodilý rozdíl.

Ověřeno `getComputedStyle` na 768/1023px – všech 6 zkoumaných sekcí teď 64/64px (kromě těch dvou záměrných výjimek) – a zpětně na mobilu (390px, beze změny, pořád vlastní 48px unifikace ze sekce 15) a desktopu (1024/1440px, beze změny, původní odstupňované hodnoty 48/64/96–112px).

**Uživatelka potvrdila jako doladěné.** Viz i nové standardní pravidlo v sekci "Pravidla pro budoucí práci" nahoře – padding sekcí hlídat na všech třech šířkách (desktop/tablet/mobil) při každé nové/upravené sekci, ne až dodatečně na vyžádání.

## Favicon – nová, vytvořená z motivu firemního loga

Uživatelka chtěla favicon postavenou na skutečném firemním logu (gear/swoosh mark), s bílým motivem na tmavě modrém poli – ne na generické vektorové ikoně, která tam byla dřív (`favicon.svg`, jednoduché ozubené kolo bez vazby na reálné logo).

- **Zdroj:** `Obrazky/web/logo-mark-white.png` (468×266, bílá RGBA varianta gear+swoosh mark s proměnlivou alfou). Sestaveno přes Python/Pillow (na stroji dostupný `python3` + Pillow 11.3.0, žádný `convert`/`magick`).
- **Chyba a oprava — "moc položené" kolo:** první verze ořezávala mark jen na pixely s alfou > 150 (tvrdý práh), což odstranilo jemné poloprůhledné "stínové" prstence pod ozubeným kolem (v barevném logu šedé, v bílé verzi nízko-alfa) – v reálném logu právě tyhle prstence dávají dojem hloubky/rotace, bez nich to vypadalo jako placaté položené kolo. **Fix:** zachován původní alfa kanál beze změny (jen barva sjednocena na bílou, alfa z originálu ponechána), takže mark teď obsahuje i tyhle jemné prstence a odpovídá reálnému logu (ověřeno i přímým porovnáním s `Obrazky/logo_raska_engineering.png` – stejný úhel/tvar mark, žádná distorze).
- **Pozadí:** gradient identický s patičkou webu (`linear-gradient(160deg, #163a68 0%, #163a68 55%, #1F4E8C 100%)`), přepočítaný v Pillow projekcí každého pixelu na směrový vektor úhlu 160°, ne plná barva jako v první verzi.
- **Zaoblené rohy:** alfa maska s `radius = 0.20 * CANVAS`.
- **Velikost motivu v ploše doladěna iterativně** dle zpětné vazby: zprvu ~66 % šířky plátna (moc velký padding), nakonec zvětšeno na 88 % šířky (jen malý okraj).
- **Zjednodušení počtu souborů:** první verze generovala celou sadu (16/32/48/180/192/512 px PNG + multi-size `.ico` + `apple-touch-icon.png`). Uživatelka se zeptala, proč tolik velikostí, když 512 px stačí – zjednodušeno na jediný soubor `Obrazky/web/favicon.png` (512×512, s obsahem posledních dvou oprav výše), referencovaný v `index.html` (`<link rel="icon">` + `<link rel="apple-touch-icon">` na stejný soubor – moderní prohlížeče/OS si ho zmenší samy). Všechny ostatní velikosti i starý `favicon.svg` smazány.
- Ověřeno vizuálně (náhled výsledného PNG i zoomované 16/32px verze pro čitelnost v malém rozlišení) a otevřením `index.html` v prohlížeči přes `file://`.

**Aktuální stav:** `Obrazky/web/favicon.png` je jediný favicon soubor v projektu, reference v `index.html` (sekce `<head>`, `<!-- Favicon -->`).

## Otevřené / nedořešené body

- Uživatel zatím nepotvrdil finální schválení posledního stavu průmysl panelu (návrat k fotce s potrubím) – čeká se na vizuální kontrolu.
- Rozhodnout, zda smazat záložní `(kopie)` soubory a nepoužitou `prumysl.jpg`, až bude vše definitivně odsouhlaseno.
- Mobilní verze koláže nebyla v tomto kole vůbec revidována – stojí za to ověřit, zda poslední úpravy (fotovoltaika, průmysl) chce uživatel promítnout i tam.

## Nová stránka ELEKTRO SLUŽBY (`elektro-sluzby.html`) – první podstránka webu

Dosud existoval jen `index.html` – tohle je první skutečná podstránka, takže zároveň zavádí vzor pro zbývající (Fotovoltaika, Průmyslová řešení, O nás, Kontakt). Obsah je doslovně podle `instrukce.md` (sekce "Stránka ELEKTRO SLUŽBY"), design vychází ze stávajícího systému v `style.css` – nová CSS je v nové sekci **17. Podstránky — hero, nabídka služeb, elektrorevize, FAQ, CTA pás** na konci souboru.

**Nové, znovupoužitelné komponenty (budou sloužit i dalším podstránkám):**
- `.page-hero` / `.page-hero-split` / `.page-hero-content` / `.page-hero-media` – hero podstránky: tmavý brand gradient (stejná formule jako `.hero`/patička), text vlevo, fotka vpravo se šikmým řezem. Vizuálně navazuje na `.about-media`, jen zrcadlově (fotka vpravo → seřezává se levá hrana, `clip-path: polygon(4.5rem 0, 100% 0, 100% 100%, 0 100%)`) – záměrně NEkopíruje jedinečný full-bleed fotokoláž hero z homepage, ten zůstává specifický pro hlavní stránku. `padding-top: var(--header-height)` je na `.page-hero` (ne duplikovaně po breakpointech) – viz poučení o offsetu hlavičky v sekci 6/12 výše. Fotka `hero-elektroinstalace.jpg` (1200×1600, poměr přesně 3:4) se na mobilu/tabletu zobrazuje beze změny poměru stran (`aspect-ratio:3/4`, žádný ořez) – vědomě jiné řešení než tabletový `.about-media` (fixní výška 28rem), protože ten se laditelně zkracoval na živou zpětnou vazbu uživatelky, kterou tu k dispozici nemám; bezpečnější je fotku vůbec neořezávat (viz `instrukce.md`: "Zachovej poměr stran a fotografie nedeformuj").
- `.offer-grid` – mřížka skleněných karet (`.feature-item`, stejná komponenta jako O nás) pro sekci "Co nabízíme" (6 položek): 1 sloupec mobil, 2 tablet, 3 desktop.
- `.revize-icon`, `.revize-subhead` – drobný odznak (ikona `icon-shield` v akcentovém kruhu, stejná receptura jako `.feature-item-icon`/`.band-icon`) a mezisekční nadpisy uvnitř sekce Elektrorevize. Pro "Provádíme revize" (4 položky) znovu použit přímo `.feature-grid` (stejné komponenty jako O nás, ne nová třída) – ale jen v této sekci musí mít nulový `margin-top` (`.revize-section .feature-grid`), protože zděděné `-3.5rem` z O nás počítalo s přesahem přes fotku, která tady není.
- `.steps` / `.step-number` – 3 číslované kroky ("Jak elektrorevize probíhá"), číslo v akcentovém kruhu (stejná gradientová receptura). 1 sloupec mobil/tablet, 3 sloupce od 768px. Bez spojovací čáry mezi kroky – vyhodnoceno jako zbytečně křehké (fixní pozicování), vynecháno.
- `.faq-grid` / `.faq-item` – accordion na nativních `<details>`/`<summary>` (bez JS), skleněná karta stejná jako ostatní. Šipka `icon-chevron-down` rotuje přes `.faq-item[open] .faq-icon { transform: rotate(180deg) }`. 4 položky → od 768px dva sloupce (dle zadání "pokud je položek více než 3, rozděl je do dvou sloupců").
- `.page-cta` / `.page-cta-contact` – závěrečný CTA pás (tmavý gradient, telefon/e-mail jako odkaz s ikonou + jedno tlačítko "Nezávazná poptávka" vedoucí na `/kontakt`).

**Nové SVG ikony přidané do sprite (`icon-chevron-down`, `icon-bulb`, `icon-panel`, `icon-smart-home`, `icon-house`, `icon-renovate`)** – stejný styl jako stávající (24×24 viewBox, stroke 1.8, zaoblené rohy). Zároveň se poprvé využily i dřív nepoužité ikony `icon-bolt`, `icon-factory`, `icon-shield`, `icon-tools` (byly ve spritu na homepage připravené, ale nikde odkazované – zjevně předpřipravené pro podstránky).

**Obrázky:** hero přebírá už existující `Obrazky/web/hero-elektroinstalace.jpg` (1200×1600, technik u rozvaděče). Galerie realizací použila 4 už připravené soubory (`realizace-elektro.jpg/-2/-3/-4`) a doplnila 2 nové (`realizace-elektro-5.jpg`, `-6.jpg`, 768×1024) – ořezané 1:1 (beze změny, zdroje už měly poměr přesně 3:4) ze surových fotek ve `Obrazky/Elektroinstalace/` (`A4AABD71...` – rozvaděč s elektroměry v bytovém domě, `ED08E3C1...` – venkovní elektroměrová skříň na tyrkysové zdi), vybrané pro vizuální odlišnost od zbytku sady (celý zbylý raw fond jsou z velké části téměř identické záběry stejného rozvaděče/rozvodné skříně z jedné zakázky).

**Odkazování:** hero tlačítko "Nezávazná poptávka" i závěrečné CTA tlačítko vedou přímo na `/kontakt` (stránka zatím neexistuje, čeká na vytvoření) – původně hero tlačítko mířilo na `#poptavka` uvnitř stránky, ale ta sekce žádný formulář nemá (jen odkaz na kontakt), takže by šlo o zbytečný dvojitý skok; opraveno na přímý odkaz.

**Structured data:** kromě `Electrician`/LocalBusiness (stejná jako homepage) přidán `FAQPage` JSON-LD se 4 otázkami/odpověďmi ze sekce "Otázky a odpovědi" – relevantní dle `instrukce.md` ("Vytvoř strukturovaná data ... FAQ ... pouze pokud jsou relevantní").

**Ověřeno Playwright screenshoty** na 390/820/1440px (žádné horizontální přetečení), včetně funkčního testu accordionu (otevření, rotace šipky) a lightboxu galerie (otevření, popisek).

**Nevytvořeno v tomto kole (mimo rozsah zadání "navrhni podstránku"):** `.htaccess`, `sitemap.xml`, `robots.txt`, `llms.txt`, čisté URL rewrite pravidla – web pořád běží jen jako `index.html` + teď `elektro-sluzby.html`, testováno přes `file://`. Interní odkazy (nav, patička, CTA) používají čisté URL (`/elektro-sluzby`, `/kontakt` atd.) stejně jako homepage – při testování přes `file://` proto proklik mezi stránkami z menu nebude fungovat, dokud nebude existovat skutečný hosting s rewrite pravidly (stejné omezení jako už dřív u homepage odkazů na neexistující podstránky).

### Druhé kolo úprav podstránky – hero přestavěn, přidána fotka do obsahu

Zpětná vazba uživatelky po první verzi: hero nesmí stát na fotce na výšku (nebo musí být výškově omezená), použít radši výřez elektro panelu přímo z homepage koláže, dát textu víc místa než fotce (texty podstránek jsou dlouhé), a hero podstránek má být menší/nenápadnější než homepage – ta má zůstat vizuálně největší a nejmarkantnější. Obsahová část navíc chtěla oživit fotkou, ideálně recyklací už hotové komponenty (konkrétně zmíněna "O nás" na homepage).

- **Nalezen zdroj elektro panelu z koláže.** `hero-collage-desktop.jpg` je poskládaná ze 4 samostatných zdrojových fotek (viz "Hero fotokoláž" výše) – elektro panel byl jediný, u kterého stav.md neuváděl zdroj ("beze změny, původní"). Ověřeno vzorkováním – panel odpovídá `Obrazky/kolaz/close-up-circuit-breakers-wire-control-panel.jpg` (5472×3648, stock fotka jističů s barevně odlišenými vodiči a kabelovými vázačky). Použití tohoto už jednou schváleného/použitého assetu na nové podstránce nepočítám jako "novou stock fotku bez svolení" – jde o stejný zdroj, jaký už reálně je na homepage, jen jinak oříznutý, přesně podle instrukce uživatelky.
- **Nový hero obrázek `Obrazky/web/hero-elektro-panel.jpg`** (1800×1555, poměr ~1.16) – ořez `(1250, 0, 5472, 3648)` ze zdrojové fotky výše (odstraňuje většinu rozostřené prázdné zdi vlevo, ponechává jističe + barevné vodiče se sponami, které jsou v koláži vidět).
- **`.page-hero` přestavěn (`style.css`, sekce 17):** zrušeno `min-height: calc(100vh - header)` na desktopu – výška řádku teď plyne čistě z obsahu (padding + přirozená výška nadpisu/textu/tlačítek), ne z výšky okna. Poměr sloupců otočen z 46/54 (text/fotka) na **58/42** – textu podstránek (delší nadpisy i odstavce než krátký claim homepage) je teď vždy víc místa. `.page-hero-media` z portrétového `aspect-ratio:3/4` (odpovídalo staré, na výšku orientované fotce) přepnuto na krajinné `aspect-ratio:16/10` na mobilu/tabletu. Výsledek je čitelně menší a klidnější sekce než homepage hero (ten zůstal beze změny) – přesně dle požadavku "podstránky trošku menší, méně nápadné, jen v podobném stylu".
- **Sekce Elektrorevize přestavěna na komponentu `.about-split`/`.about-content`/`.about-media`/`.feature-grid`** – stejná trojice tříd jako homepage "O nás" (fotka se šikmým řezem, vodoznak loga, karty přesahující přes spodní hranu fotky), místo dřívější ploché varianty na `.section--blue` s odznakem ikony. Fotka: `hero-elektroinstalace.jpg` (1200×1600, technik u rozvaděče) – uvolněná z hero sekce po přechodu na `hero-elektro-panel.jpg`, teď slouží jako obsahová fotka. Nadpis "Provádíme revize" nad 4kartovou mřížkou je nově `<h3 class="sr-only">` (stejný vzor jako sr-only `<h2>` před kartami služeb na homepage) – `.feature-grid` totiž záměrně přesahuje přes spodek fotky (`margin-top:-3.5rem`, stejně jako u O nás) a viditelný nadpis by do toho přesahu narážel.
- **Odstraněno:** `.revize-icon` (odznak s ikonou štítu před nadpisem Elektrorevize) – ve stylu "O nás", který se teď kopíruje, žádný odznak není, takže byl nekonzistentní; smazán z HTML i CSS. Zrušeno i dřívější `.revize-section .feature-grid { margin-top: 0; }` – přesah je teď žádoucí (fotka nad mřížkou existuje), ne chyba k opravě.
- **Meta/OG/JSON-LD `image`** přepnuty z `hero-elektroinstalace.jpg` na nový `hero-elektro-panel.jpg` (teď skutečný hero obrázek stránky).
- Ověřeno Playwright screenshoty na 390/820/1440/1920px – bez horizontálního přetečení, hero viditelně nižší a s dominantnějším textovým sloupcem, sekce Elektrorevize vizuálně shodná s "O nás" (fotka, vodoznak, přesah karet) na všech šířkách.

### Třetí kolo úprav – pozadí sekcí a sjednocení Elektrorevize

Tři body zpětné vazby po druhém kole:

1. **"Máte zájem o elektro služby?" (`.page-cta`) splývala s patičkou.** Obě sekce používaly identický tmavý gradient (`linear-gradient(160deg, primary-dark...primary)`), takže mezi nimi nebyla vidět žádná hranice. **Fix:** `.page-cta` přepnuta na `var(--color-bg-blue)` (světle modrá) + tmavě modré nadpisy/tmavý text, stejný barevný jazyk jako kontaktní sekce na homepage (`.contact-section`), jen bez dvousloupcového gridu a formuláře (ty homepage kontaktní sekce nemá tato podstránka vůbec) – zůstal jen vycentrovaný text, telefon/e-mail jako odkazy a tlačítko "Nezávazná poptávka". Nad tmavou patičkou je teď jasně vidět hranice.
2. **FAQ karty na bílém pozadí "zanikaly"** – `.faq-item` je skleněná karta (`rgba(255,255,255,0.5)` + blur), na bílém `.section` pozadí prakticky splývala se stránkou. **Fix:** sekce dostala třídu `section--blue` (existující utilita, `background: var(--color-bg-blue)`) – karty teď mají viditelný kontrast, stejně jako všechny ostatní skleněné karty na webu (ty jsou vždy navržené na barevné/fotografické pozadí, ne na čistě bílé).
3. **Elektrorevize – příliš mnoho oranžových kruhů, sekce působila rozdělená na dva kusy.** Sekce měla 7 oranžových kruhů (4 ikony karet "Provádíme revize" + 3 číslované kroky) a vizuálně se dělila na modrý "úvodní" blok (`.about-split`) a bílý "seznamový" blok pod ním (kontejner s kartami a kroky), protože jen `.about-split` měl vlastní modré pozadí, zbytek sekce byl na běžném bílém pozadí stránky. **Fix:**
   - `.step-number` (kroky "Jak elektrorevize probíhá") zbaven kruhu/gradientního pozadí – teď je to jen velké tučné číslo v akcentové barvě (`font-size:2rem; font-weight:800; color:accent`), bez "puntíku". Ikonové odznaky ve "Provádíme revize" (skutečné ikony, ne číslice) ponechány beze změny – u nich kruhový odznak dává smysl jako nosič ikony.
   - Nové pravidlo `.revize-section { background: var(--color-bg-blue); }` – obarví CELOU sekci (i část pod `.about-split`) stejnou modrou, jakou už měl samotný `.about-split`, takže zmizel švík a sekce teď působí jako jeden ucelený blok od fotky až po poslední krok.
- Ověřeno Playwright screenshoty na 390/1440px (bez horizontálního přetečení) – FAQ karty viditelně kontrastní, CTA pás jasně oddělený od patičky, Elektrorevize jedna souvislá modrá plocha bez švu, jen 4 oranžové ikonové odznaky místo 7 kruhů.

### Čtvrté kolo úprav – oranžové karty v "Co nabízíme", kompaktnější Elektrorevize

Dva další požadavky:

1. **"Co nabízíme" – návrh uživatelky:** bílé karty na bílém pozadí podbarvit světlou oranžovou (`--color-bg-orange`), ať na bílé ploše sekce vyniknou, a přebarvit ikonu na modrou (oranžová ikona na oranžové kartě by měla nízký kontrast). Zavedeno jako scoped pravidlo `.offer-grid .feature-item` (jen tahle mřížka, ne sdílený `.feature-item` používaný jinde na modrém pozadí u "Provádíme revize" a homepage "O nás", kde oranžová ikona/bílá karta fungují dobře beze změny): `background: var(--color-bg-orange)` + `.offer-grid .feature-item-icon` přebarvena na modrý gradient (`primary → primary-dark`, stejná receptura jako `.showcase-tag`). Karty teď na bílé ploše viditelně vynikají, dvojice oranžová karta/modrá ikona drží obě brandové barvy pohromadě.
2. **Elektrorevize "zkompaktnit celou sekci"** – zmenšeny/zúženy prostory napříč celou sekcí (přebito přes potomkové selektory s vyšší specificitou, ne přes pořadí v souboru, kvůli spolehlivosti): `.revize-section` padding-block ze `--space-16`/mobilní `--section-padding-mobile` sjednoceno na `--space-8` (32px, uniformně na všech šířkách – uživatelka chtěla kompaktnost napříč, ne jen na jedné šířce); `.revize-section .about-content` padding-block na `--space-8` (dřív 48–64px podle šířky); `.revize-section .about-split` na desktopu (1024px+) `min-height: 26rem` místo sdílených `36rem` (stejná nižší hodnota, jaká se osvědčila na tabletu u homepage O nás); `.revize-section .feature-grid` přesah zmenšen z `-3.5rem` na `-2.5rem`; `.revize-subhead` margin-top/-bottom zmenšeny ze `12/6` na `8/4` (na `--space` škále); `.steps` gap zmenšen z `8` na `6` (mobil) a z `6` na `4` (768px+). Sekce teď působí jako jeden hutný celek, ne rozvleklá řada velkých bloků – ověřeno, že se celá vejde do jedné obrazovky na běžném desktopu (1440×900) beze scrollování.
- Ověřeno Playwright screenshoty na 390/1440px – karty "Co nabízíme" viditelně kontrastní na bílém pozadí, Elektrorevize znatelně nižší/hutnější, žádné horizontální přetečení.

### Páté kolo úprav – oranžové karty bez rámečku, přestavba Elektrorevize na text+karty/foto, odlišení nadpisů

Tři další body zpětné vazby:

1. **"Co nabízíme" – karty bez rámečku, ikony bez kolečka.** `.offer-grid .feature-item` dostala `border:none` a vlastní (oranžově tónovaný, ne sdílený modrý) `box-shadow`. Ikony ztratily kruhové modré pozadí (`.offer-grid .feature-item-icon { background:none; box-shadow:none; width/height:auto }`), zvětšily se (3rem SVG místo 1.4rem v 3.5rem kruhu) a dostaly stejný "glow" `drop-shadow` efekt jako `.service-card-icon img` pod hero na homepage (bílý halo + jemný oranžový stín), jen v menším měřítku – přesně "efekt jako na hero", který uživatelka navrhla jako alternativu.
2. **Elektrorevize přestavěna na text+karty vlevo / foto vpravo.** Předchozí verze (převzatá `.about-split`) měla karty jako plnou šířku PŘES fotku dole – uživatelka chtěla karty "Provádíme revize" hned pod textem VE STEJNÉM (levém) sloupci, ve 2×2 mřížce, a fotku na druhou (pravou) stranu – tj. layout obrácený oproti homepage "O nás" (tam foto vlevo/text vpravo). Vytvořena nová sada tříd (`.revize-split`/`.revize-content`/`.revize-cards-grid`/`.revize-media`) místo recyklace `.about-split` (ta strukturálně nepočítala s kartami uvnitř textového sloupce) – šikmý řez fotky ale používá stejnou techniku/hodnoty jako `.page-hero-media` výše na téže stránce (`clip-path: polygon(4.5rem 0, 100% 0, 100% 100%, 0 100%)`), jen zrcadlově stejným směrem (foto vpravo v obou komponentách).
   - **Nalezená a opravená chyba (CSS Grid overflow na mobilu):** `.revize-cards-grid { grid-template-columns: repeat(2, 1fr) }` způsobilo horizontální přetečení stránky o 11px na 390px šířce – `1fr` track má defaultní `minmax(auto, 1fr)`, takže se nemůže zúžit pod min-content šířku textu v kartě. Opraveno na `repeat(2, minmax(0, 1fr))` (standardní oprava tohoto konkrétního grid chování). Ověřeno `document.documentElement.scrollWidth` na 390/820/1440px po opravě – všude přesně na šířku viewportu.
3. **Odlišení nadpisů ("Jak to probíhá... podobné nadpisy všude, stejná barva, stejné písmo").** Nadpisy "Provádíme revize" a "Jak elektrorevize probíhá" (dřív plnohodnotné h3 stejné navy barvy/řezu jako H2 "Elektrorevize" i názvy karet) přestylovány na `.revize-subhead` v eyebrow stylu (malé kapitálky, tučné, oranžová, `letter-spacing`) – zůstávají skutečné `<h3>` (sémantika/nadpisová osnova beze změny), jen vizuálně úplně jiný typografický hlas než okolní navy nadpisy. U kroků "Jak elektrorevize probíhá" navíc změněna kompozice – číslo a název kroku jsou teď v jednom řádku vedle sebe (`.step-heading`, flex řádek), místo čísla v kolečku nad samostatným nadpisem jako předtím – další strukturální (ne jen barevná) odlišnost od zbytku sekce.
- Ověřeno Playwright screenshoty na 390/820/1440px – bez horizontálního přetečení, layout přesně dle zadání (text+2×2 karty/foto na desktopu, rozumné skládání na tabletu i mobilu), tři jasně odlišené typografické úrovně v sekci Elektrorevize (H2, eyebrow, karta/krok).

### Šesté kolo úprav – Elektrorevize kompletně přestavěna (fotka pryč, ploché karty)

Uživatelka: sekce pořád vypadá "hrozně", chce ji modernizovat. Konkrétně: fotka (technik u rozvaděče) je obrovská A tematicky neodpovídá – na fotce probíhá MONTÁŽ, ne REVIZE. Oranžové ikony (glow efekt z minulého kola) jsou taky "hrozné" – zmenšit, dát doleva (vedle textu, ne nad něj), přepracovat vnitřní rozestupy karet, "inspirovat se na netu".

- **Prověřeny dostupné fotky pro téma "měření/revize"** (`Obrazky/Pohotovost/pohotovost-1..4.jpg`) – `pohotovost-3.jpg` (technik s multimetrem, čtená hodnota na displeji) by tematicky seděl nejlépe, ale vizuálně jde o stylizovanou/nasvícenou fotku bez firemního brandingu (na rozdíl od skutečných realizačních fotek s Raška logem na oblečení) – nejistá provenience (možná stock), a `pohotovost-4.jpg` (jistě uživatelčina vlastní fotka dle poznámky z dřívějška) se navíc už používá přímo v pásu "Potřebujete elektrikáře rychle?" hned nad touto sekcí, takže by se stejná fotka opakovala dvakrát na jedné stránce. Vzhledem k nejistotě a riziku duplicity zvoleno **řešení bez fotky** – bezpečnější než hádat/riskovat nevhodný/neautorizovaný snímek, navíc přímo řeší i "fotka je obrovská".
- **Sekce přestavěna na plochý, bezfotkový layout:** `.revize-section` teď používá stejný vzor jako "Co nabízíme" a "Otázky a odpovědi" na téže stránce – `.section.section--blue` + centrovaný `.section-head.section-head--center` (H2 + lead) – sjednocuje to vzhled úvodu napříč celou stránkou (dřív byla Elektrorevize jediná sekce s netypickým levostranným úvodem vedle fotky).
- **Karty "Provádíme revize" kompletně přepracované** – smazány `.revize-split`/`.revize-content`/`.revize-media` (celá fotková komponenta), nové `.revize-card`/`.revize-card-icon`/`.revize-card-body`: řádkový layout (ikona vlevo, text vpravo, `align-items:flex-start`), žádný rámeček, žádný stín, jen jemný poloprůhledný bílý pruh (`rgba(255,255,255,0.55)`) s malým `border-radius` (14px, ne 24px jako těžší karty jinde – plošší, "modernější" dojem). Ikona zmenšena z 3rem (s glow efektem) na 1.6rem, bez kolečka, bez filtru – čistá oranžová linková ikona. Padding zúžen na `var(--space-4)` (16px) místo dřívějších 24–32px – kompaktnější řádky. Grid 2 sloupce od 640px, 1 sloupec pod tím (předtím byl 2 sloupce i na nejmenších mobilech, na velmi úzkém displeji by řádkový layout ikona+text byl stěsnaný).
- Ověřeno Playwright screenshoty na 390/820/1440px – bez horizontálního přetečení, sekce vizuálně výrazně klidnější/plošší, konzistentní s centrovaným úvodem zbytku stránky.

### Sedmé kolo – oprava přehnaného "zploštění" karet z minulého kola

Uživatelka po pátém kole: sekce vypadá ještě hůř, "co to má společného s homepage? Úplně jiná stránka, katastrofa." Po vizuálním porovnání screenshotů vedle sebe (homepage `.about-section`/`.services-overlap` vs. nová `.revize-card`) se ukázalo, že minulé kolo bylo skutečně chybné rozhodnutí, ne otázka vkusu: karty "Provádíme revize" úplně ztratily skleněný efekt (blur, `shadow-lg`, border), přišly o barevné gradientové kolečko na ikonu a padding zúžily na minimum – vedle prémiových skleněných karet s gradientovými odznaky, které se používají doslova všude jinde na webu (homepage O nás, karty služeb, "Co nabízíme" na téže stránce), to působilo jako placatý, "levný" seznam z úplně jiného webu. Přílišná snaha o "moderní/plochý" vzhled minulé kolo zlikvidovala základní vizuální identitu webu popsanou v `instrukce.md` ("web má působit prémiově").

**Fix:** `.revize-card`/`.revize-card-icon` přepsány tak, aby používaly PŘESNĚ stejnou recepturu jako `.feature-item`/`.feature-item-icon` (`rgba(255,255,255,0.5)` + `blur(28px) saturate(160%)` + `border` + `box-shadow: var(--shadow-lg), inset...` + `border-radius: var(--radius-lg)`, ikona jako gradientové kolečko `accent → accent-dark` se stínem) – jediný rozdíl oproti běžnému `.feature-item` je řádkový layout (`display:flex; align-items:center`, ikona vlevo místo nahoře) a mírně menší odznak ikony (3rem místo 3.5rem), přesně podle požadavku "zmenši, dej doleva". Padding vrácen na `var(--space-6)` (24px, stejně jako `.feature-item`).

**Poučení pro příště:** při "zmodernizování" komponenty na žádost uživatelky vždy nejdřív zkontrolovat vizuálně vedle jiné, už schválené části webu (ideálně screenshotem obou vedle sebe) – ne se řídit jen abstraktní představou "co je dnes moderní na netu". Tenhle web má už definovaný vlastní vizuální jazyk (skleněné karty, gradientové odznaky, měkké stíny) a "modernizace" v něm znamená doladit kompozici/velikosti v jeho rámci, ne nahradit ho jiným (plochým, bezstínovým) stylem.
- Ověřeno Playwright screenshoty na 390/1440px – karty vizuálně konzistentní s "Co nabízíme" i s homepage O nás/kartami služeb, bez horizontálního přetečení.

### Osmé kolo – vrácen ověřený vzhled (screenshoty od uživatelky), fotka vyměněna za tematicky sedící

Uživatelka poslala 3 screenshoty staršího stavu (před koly 5–6) a požádala "vrať tento vzhled" – potvrzeno přes `AskUserQuestion`, že chce vizuální STYL z těch screenshotů (velké skleněné karty, ikony v kolečku, foto+text nahoře s kartami přesahujícími přes fotku), ale s jinou fotkou – ta na screenshotu byla znovu ta stejná nevhodná fotka montáže rozvaděče (viz šesté kolo), kterou uživatelka sama předtím odmítla.

- **"Co nabízíme"** – ikony vráceny z plochého oranžového "glow" stylu (kolo 5) zpátky na modré gradientové kolečko (`primary → primary-dark`, stejná receptura jako `.feature-item-icon` jinde) – karta samotná (oranžový podtón, bez rámečku) zůstala.
- **Elektrorevize** – vráceno ke komponentě `.about-section`/`.about-split`/`.about-content`/`.about-media`/`.about-watermark`/`.feature-grid` (fotka + text nahoře, karty "Provádíme revize" přesahující přes spodní hranu fotky, ikony zpět v plných oranžových kolečkách, `sr-only` nadpis nad kartami, kroky "Jak elektrorevize probíhá" zpět na číslo NAD nadpisem místo řádku vedle sebe) – identická komponenta a kompaktní rozměry jako ve třetím kole, které uživatelka na screenshotu ukázala jako žádoucí stav.
- **Nová fotka místo té nevhodné:** `Obrazky/web/revize-mereni.jpg` (1800×1200, přepočteno z `Obrazky/Pohotovost/pohotovost-4.jpg` – ověřeně vlastní/reálná fotka firmy, ne stock) – technik s multimetrem při měření na rozvaděči. Tenhle motiv (kontrola/měření) tematicky sedí k "elektrorevize" mnohem líp než dřívější fotka montáže. Stejný zdrojový snímek se už používá (jinak oříznutý) v pásu "Potřebujete elektrikáře rychle?" výše na stránce – vzhledem k tomu, že jde o jedinou ověřeně vlastní fotku s motivem měření, které měla firma k dispozici, je opakované použití přijatelnější než riziko nasazení neautorizované/stock fotky.
- Ověřeno Playwright screenshoty na 390/820/1440px – vzhled odpovídá referenčním screenshotům od uživatelky, bez horizontálního přetečení.

**Poučení pro příště:** než se něco na žádost uživatelky výrazně předělá ("zmodernizuj", "zkompaktni"), je bezpečnější dělat menší, ověřitelné kroky a průběžně srovnávat s already-schváleným vzhledem (screenshotem vedle sebe), místo jedné velké reinterpretace najednou – tahle sekce prošla 4 kola sem a tam (fotka pryč → plochý styl → zpátky na fotku a skleněné karty), což šlo z velké části předejít opatrnějším postupem a včasným ptaním se, když protichůdné požadavky (žádná nevhodná fotka × "vrať tento vzhled" se stejnou fotkou) vyžadovaly vyjasnění.

### Deváté kolo – konkrétní fotka pro Elektrorevize, větší rozestupy v krocích

Uživatelka vybrala konkrétní fotku (`Obrazky/Dalsi foto/55091869-4F69-42BE-BF3D-5BA17D9FDB1E-1024x768.jpg`) – otevřený průmyslový rozvaděč s multimetrem a zkoušečkou napětí položenými dole, čekajícími na měření. Zkopírována (beze změny rozměrů, 1024×768) do `Obrazky/web/revize-mereni.jpg` (přepsala dřívější verzi stejného názvu). `object-position` doladěna na `center 75%` – nástroje (nejdůležitější detail pro téma revize) jsou ve zdrojové fotce dole, vyšší Y hodnota odkryje víc spodku a míň horní police/poznámkového bloku.

**Rozestupy v "Jak elektrorevize probíhá":** uživatelka nahlásila, že kroky působí "nalepené". Zvětšeno: `.steps` gap z `--space-6`/`--space-4` (24px/16px) na `--space-8` (32px) na všech šířkách; `.revize-subhead` margin-bottom (mezera před prvním krokem) z `--space-4` na `--space-6`; `.step-number`/`.step-item h4` margin-bottom z `--space-2` na `--space-3`. Ověřeno screenshoty na 390/1440px – kroky teď mají znatelně vzdušnější odstup od sebe i uvnitř (číslo → nadpis → text).

### Desáté kolo – "divný pruh" u fotky (moc nízká), ještě větší mezera u kroků

Uživatelka poslala screenshot s "divným pruhem" u fotky v Elektrorevize a požádala o víc výšky; zároveň chtěla ještě větší mezeru u "Jak elektrorevize probíhá" (z devátého kola nestačilo).

- **Diagnostika:** problém nebyl (jen) na tabletu, ale hlavně na užších desktopech (1024–1200px) – `.revize-section .about-split { min-height: 26rem }` (zavedeno v kole "kompaktnost", viz šesté kolo) dávalo při typické šířce fotkového sloupce na těchto rozlišeních (~500–600px) poměr až 1.5:1, takže fotka vypadala jako plochý úzký pruh místo fotky. Ověřeno screenshoty přímo na 1024/1080/1200px – problém tam byl zřetelně vidět.
- **Fix:** `min-height` na desktopu (1024px+) zvýšena z `26rem` na `34rem` – blíž k hodnotě `36rem`, kterou pro identickou komponentu (fotka+text, šikmý řez) používá homepage O nás, jen mírně kompaktnější. Navíc přidán samostatný scoped override i pro tablet (768–1023px), kde sdílené pravidlo `.about-media { height: 28rem }` (laděné na užší sloupec homepage O nás) dávalo na plnou šířku obrazovky této sekce podobně "useknutý" poměr – zvýšeno na `34rem` i tam (`.revize-section .about-watermark` bottom offset přepočítán souběžně).
- **Mezera u kroků zvětšena podruhé:** `.steps` gap z `--space-8` (32px) na `--space-12` (48px) na všech šířkách; `.revize-subhead` margin-top (odstup od karet "Provádíme revize" nad tím) z `--space-8` (32px) na `--space-16` (64px, zdvojnásobeno).
- Ověřeno Playwright screenshoty na 768/1023/1024/1440/390px – fotka má na všech těchto šířkách přirozený, ne "pruhový" poměr stran, mezera u kroků citelně větší, bez horizontálního přetečení. `.page-hero-content h1`/`.lead` neměly vlastní `font-size`, takže dědily obecnou `--fs-h1`/`.lead` škálu – na desktopu (1440px) z toho vyšel nadpis podstránky **60px, tedy VĚTŠÍ než homepage hero (48px)**, přesný opak zadání "homepage musí zůstat dominantní, podstránka menší". Přepočítáno přes `clamp()` na konkrétních šířkách (390/820/1440px) před i po opravě – viz čísla v konverzaci. **Fix:** `.page-hero-content h1 { font-size: clamp(1.75rem, 1.5rem + 1.6vw, 2.5rem); }` (30–40px, cca 75–83 % homepage hodnoty na všech šířkách) a `.page-hero-content .lead { font-size: 1rem; }` (pevných 16px místo dřívější stejné škály jako homepage lead). Ověřeno srovnávacím screenshotem homepage vs. `elektro-sluzby.html` na 390 i 1440px.

### Jedenácté kolo – přiblížení hero fotky rozvaděče, pořadí sekcí a styl karet "Co nabízíme" sladěny s homepage

Tři samostatné požadavky uživatelky v jedné zprávě.

- **Hero fotka rozvaděče přiblížena.** Nový ořez ze stejného zdroje (`Obrazky/kolaz/close-up-circuit-breakers-wire-control-panel.jpg`, box `(1900,0,5472,3648)` místo dřívějšího `(1250,0,5472,3648)`) – odstraňuje většinu rozostřeného bílého plastového žlabu/lišty vlevo, který v předchozím ořezu zabíral značnou část záběru a nebyl součástí rozvaděče. Uloženo jako `Obrazky/web/hero-elektro-panel.jpg` (1700×1736, dřív 1800×1555).
  **Vedlejší objev – skutečný CSS bug:** `.page-hero-media` mělo nastavený `aspect-ratio`, ale `.page-hero-media img { width:100%; height:100% }` uvnitř (column) flex kontejneru způsobovalo, že se `aspect-ratio` na rodiči vůbec neuplatnil – box se ve skutečnosti řídil nativním poměrem stran zdrojové fotky (ověřeno izolovanou reprodukcí: div bez img se choval podle `aspect-ratio` správně, se stejným img uvnitř ne). Fotka se tedy na mobilu/tabletu od začátku zobrazovala v jiném poměru, než jaký `aspect-ratio: 16/10` (resp. `4/3`) deklarovalo. **Fix:** `img` přepnut na `position:absolute; inset:0` (rodič má `position:relative`) – standardní bezpečný vzor pro aspect-ratio box, `aspect-ratio` teď skutečně platí. Hodnota nastavena přesně na poměr nového ořezu (`1700/1736`), aby CSS `cover` neořezával navíc nahoře/dole – jističe jsou už ve zdrojové fotce seříznuté nahoře i dole fotograficky, širší poměr by o kus další řady přišel (ověřeno – zkoušeno nejdřív s `4/3`, to uřízlo celou horní řadu jističů, vráceno na poměr zdroje).
  Ověřeno Playwright screenshoty (crop elementu `.page-hero-media`) na 390/768/1440px – obě řady jističů + barevné vodiče teď zabírají výrazně větší část záběru, min rozostřeného pozadí vlevo, bez ztráty žádné řady.
- **Pořadí sekcí sjednoceno s homepage.** Na homepage jde `services-overlap` (karty služeb) → `.band` (pohotovost) → O nás; na `elektro-sluzby.html` bylo pořadí obráceně (`.band` → "Co nabízíme"). Prohozeno – teď "Co nabízíme" → `.band` → Elektrorevize, stejné pořadí typů sekcí jako na homepage.
- **Karty "Co nabízíme" sjednoceny s homepage 1:1.** Zrušen celý scoped override `.offer-grid .feature-item`/`.feature-item:hover`/`.feature-item-icon` (oranžový podtón karty + modré gradientové kolečko ikony, zavedený v pátém/sedmém kole) – karty teď dědí čistě základní `.feature-item` recepturu stejnou jako `.feature-grid` v homepage O nás (bílé sklo, oranžové gradientové kolečko ikony). Žádné vlastní přebarvení této sekce.
- Ověřeno Playwright screenshoty (`data-reveal` prvky pro screenshot dočasně force-`is-visible`, jinak IntersectionObserver na statickém screenshotu níže na stránce nic neodkryje) na 390/1440px – pořadí sekcí i vzhled karet odpovídají homepage, bez horizontálního přetečení.

### Dvanácté kolo – hero fotka oddálena, "Co nabízíme" přesahuje do hero (jako homepage), oprava pruhu u fotky Elektrorevize, šipkový design kroků, větší mezery

Uživatelka v jedné zprávě upřesnila/opravila směr předchozího kola (hero fotka měla být oddálena, ne přiblížena) a přidala čtyři další požadavky.

- **Hero fotka rozvaděče výrazně oddálena.** Minulé kolo ji naopak přiblížilo (ořez od x=1900) – uživatelka chtěla přesný opak ("hodně oddal, aby bylo vidět víc"). Nový ořez ze stejného zdroje, tentokrát širší než PŮVODNÍ verze z desátého kola: `(700,0,5472,3648)` místo `(1900,0,...)` i místo původního `(1250,0,...)` – zabírá podstatně širší část rozvaděče (obě řady jističů + celý svazek barevných vodičů). Uloženo jako `hero-elektro-panel.jpg` (1900×1452, poměr ~1.31). `.page-hero-media { aspect-ratio }` přepočítáno na přesný poměr nového ořezu (`1900/1452`), aby CSS `cover` (po opravě bugu z minulého kola) neořezával navíc.
- **"Co nabízíme": nadpis skryt, karty přesahují do hero sekce (jako `.services-overlap` na homepage).** Viditelný `<h2>Co nabízíme</h2>` nahrazen `<h2 class="sr-only">` (stejný vzor jako sr-only nadpis nad kartami služeb na homepage). Sekce přejmenována ze `.section` na novou `.offer-overlap` – záporný `margin-top` (-5rem mobil / -6rem tablet / -7rem desktop, menší hodnoty než homepage -8.6/-9.6/-10.6rem, protože hero podstránky je menší) vytáhne mřížku karet nahoru přes spodní hranu `.page-hero`. Aby karty ležely na modrém gradientu hero, ne na bílém pozadí, `.page-hero` dostal `padding-bottom: var(--space-16)` (rezerva stejná technika jako `.hero-inner` na homepage). Ikony/karty samotné beze změny – zůstala už homepage-identická `.feature-item` receptura z jedenáctého kola (uvažováno i přesné převzetí `.service-card` vč. velkých bezkolečkových ikon jako u homepage karet služeb, ale ty jsou navržené pro velké barevné PNG ilustrace; naše 6 položek mají jen malé jednobarevné SVG linky, které by bez kolečka na tuhle velikost vizuálně nefungovaly – ponechána gradientová kolečka).
- **Oprava "světle modrého pruhu" nad fotkou Elektrorevize.** Skutečná příčina: `.revize-section` i `.about-split` uvnitř mají STEJNÉ pozadí (`--color-bg-blue`) – `.revize-section{padding-block:var(--space-8)}` posouvalo celý `.about-split` (a tedy i fotku) o 32px níž od vrchu sekce, což na jednobarevném pozadí vypadalo jako samostatný modrý pruh nad fotkou (na homepage O nás je stejná konstrukce, ale tam je pozadí sekce bílé, takže mezera splyne a není vidět). **Fix:** `padding-top` zrušen úplně (`padding-bottom` zůstal, zvětšen na `var(--space-16)` – viz mezery níže). Fotka teď sedí přesně na vrchu sekce.
- **Fotka Elektrorevize posunuta níž.** `object-position` z `center 75%` na `center 90%` – odkrývá víc spodní části zdrojové fotky (multimetr a zkoušečka napětí).
- **"Jak elektrorevize probíhá" – šipkový design kroků.** Čísla kroků přestala být holý text a staly se odznaky – gradientové kolečko (stejná receptura jako `.feature-item-icon`/`.band-icon`, `accent → accent-dark`, stín), větší a výraznější (3.25rem, tučná bílá číslice). Mezi kroky přidán malý chevron (`::after`, dvě otočené hrany pod 45°, klasická CSS "arrow" technika) – od tabletu vodorovný ">" v mezeře mezi sloupci (pozice vázaná na vertikální střed odznaku), na mobilu/užším tabletu svislý "︾" v mezeře mezi kroky pod sebou. Vizuálně čitelný postup 1 → 2 → 3 bez nutnosti JS/extra SVG markupu. (Poznámka k ladění: první verze omylem použila stejnou dvojici hran `border-bottom`+`border-right` pro OBĚ orientace, což na desktopu kreslilo "v" místo ">" – opraveno na `border-top`+`border-right` pro vodorovnou variantu.)
- **Větší mezery v sekci Elektrorevize.** `.revize-subhead` (nadpis "Jak elektrorevize probíhá") `margin-top` `var(--space-16)`→`var(--space-24)` (96px) – mezera od karet "Provádíme revize" nad tím. `.steps` `gap` `var(--space-12)`→`var(--space-16)` (64px, i kvůli místu pro nový chevron). `.revize-section` `padding-bottom` `var(--space-8)`→`var(--space-16)` (64px) – mezera před navazující sekcí "Otázky a odpovědi" (ta má navíc svých vlastních 64px padding-top z `.section`), obě sekce sdílí stejné pozadí, takže bez zvětšení mezery na sebe vizuálně nalepovaly.
- Ověřeno Playwright screenshoty (full-page, `data-reveal` force-visible) na 390/768/1440px – bez horizontálního přetečení, karty přesahují do hero na všech třech šířkách, fotka Elektrorevize flush na vrchu sekce, šipky mezi kroky správně orientované (zoom screenshot `.steps` samostatně).

### Třinácté kolo – hero fotka vrácena na původní ořez, přesah karet zmenšen (nešel přes fotku), skutečná oprava pruhu u Elektrorevize, audit všech paddingů

Uživatelka poslala screenshot hero sekce a upozornila, že karty "Co nabízíme" teď přesahují AŽ DO fotky (ne jen do prázdné plochy pod ní), že fotka rozvaděče ukazuje čím dál míň (opak toho, co chtěla), že "pohotovost" karta má prověřit padding, že Elektrorevize PŘES předchozí opravu pořád má modrý pruh nad fotkou, a požádala o celkový audit paddingů.

- **Hero fotka vrácena na přesně původní ořez** (`(1250,0)-(5472,3648)` → 1800×1555, stejný soubor jako před celým dnešním kolem úprav). Zjištěno matematicky: při tomto poměru (1.157) už zdrojová fotka využívá celou svou výšku (3648 px) – žířku dál zvětšit nejde, aniž by se poměr/výška změnily, takže tohle je zároveň NEJVÍC "oddálený" možný záběr při zachování tvaru. Předchozí dvě kola (užší i širší ořez při jiném poměru) na širokém desktopu ve skutečnosti ukazovala MÍŇ z rozvaděče – `.page-hero-media` box na desktopu nemá pevný poměr (řídí se výškou textového sloupce, ne fotkou) a při širokých viewportech (1920px → poměr boxu až ~1.9) `object-fit:cover` ořezával navíc svisle tím víc, čím dál byl poměr fotky od poměru boxu. Ověřeno přeměřením boxu (`getBoundingClientRect`) na 1920/1440px.
- **Přesah karet "Co nabízíme" zmenšen, aby nikdy nezasahoval do fotky/tlačítek.** Přeměřením zjištěno, že se záporný margin z minulého kola (-7rem desktop) natahoval AŽ 49 px do fotky (mřížka karet začínala výš než konec fotky) a měla jen ~15 px od tlačítek. Fix: `.offer-overlap` margin-top zmenšen na -2rem/-2,5rem/-3rem (mobil/tablet/desktop, dřív -5/-6/-7rem) – přeměřeno na 1920/1440/1024/768/390px, karty teď mají 15–32 px rezervu POD fotkou na všech šířkách a citelně víc místa pod tlačítky. Menší, ale bezpečný přesah – přesný "vlas nad hranou" přesah jako na homepage by vyžadoval breakpoint-přesné doladění podle výšky textového sloupce, což je křehké; zvolena konzervativnější hodnota.
- **Skutečná příčina "modrého pruhu" u Elektrorevize (napodruhé).** Předchozí kolo nastavilo `.revize-section { padding-bottom: var(--space-16) }` a spoléhalo na to, že padding-top zůstane 0, protože nebyl deklarovaný – jenže element má DVĚ třídy (`about-section revize-section`) a `.about-section { padding-block: var(--space-16) }` (64px NAHOŘE i DOLE) dál platí, protože nevynechání ≠ přebití v CSS cascade. Ověřeno `getComputedStyle` před opravou: `.revize-section` mělo `padding-top: 64px`, přestože ho vlastní pravidlo vůbec nezmiňovalo. **Skutečný fix:** `padding-top: 0` explicitně nastaveno (ne jen vynecháno). Ověřeno znovu přes `getComputedStyle` (teď `0px`) i screenshotem – fotka konečně sedí přesně na vrchu sekce.
- **Audit "pohotovost" karty a všech paddingů sekcí.** `.band`/`.band-inner` (`getComputedStyle`) má `padding: 48px` shodně na mobilu/tabletu/desktopu, identické s homepage (stejná komponenta, žádná vlastní úprava pro podstránku) – vizuálně i po zoomu na 1920/1024/768/390px vypadá konzistentně, žádná vada nenalezena. Menší optický posun textu vůči hero nadpisu (band-text začíná o 168 px víc vpravo než hero h1, kvůli ikoně+paddingu uvnitř plovoucí karty) je STEJNÝ na homepage (přeměřeno tam taky: identických 168 px) – jde o záměrnou vlastnost sdílené komponenty, ne o chybu vzniklou dnes. Projity `getComputedStyle` paddingy VŠECH sekcí stránky (`.page-hero`, `.offer-overlap`, `.band`, `.revize-section`, FAQ `.section--blue`, `.showcase-section`, `.page-cta`) – jediná skutečná anomálie byla popsaná `.about-section`/`.revize-section` kolize výše, jinde padding odpovídá deklarovaným hodnotám beze zbytku.
- **Poučení pro příště:** při psaní scoped override pravidla pro element se SDÍLENOU základní třídou (`.about-section.revize-section` apod.) nestačí "vynechat" vlastnost, kterou chci zrušit – musí se explicitně přebít (`padding-top: 0`), jinak cascade nechá hodnotu ze sdílené třídy platit dál. Zrádné právě proto, že to vypadá opraveně v kódu (nová pravidla neobsahují padding-top), ale prohlížeč ho odjinud dál dědí.
- Ověřeno Playwright screenshoty (full-page i zoom na `.revize-section`/`.band`) + `getBoundingClientRect`/`getComputedStyle` měření na 1920/1440/1024/768/390px – bez horizontálního přetečení, karty pod fotkou/tlačítky s rezervou, fotka Elektrorevize flush nahoře, žádná další padding anomálie nenalezena.

### Čtrnácté kolo – modrý pruh POD hero fotkou (důsledek zmenšeného přesahu z minulého kola)

Uživatelka nahlásila, že hero fotka "je pořád stejná" a navíc pod ní přibyl modrý pruh.

- **Soubor/CSS ověřeny přímo na disku** (`Obrazky/web/hero-elektro-panel.jpg` je 1800×1555, `.page-hero-media` má `aspect-ratio: 1800/1555`) – odpovídají přesně opravě z minulého kola. Pokud fotka v prohlížeči uživatelky vypadá nezměněná, jde nejspíš o prohlížečovou cache (stejný název souboru napříč koly, jen jiný obsah) – doporučen hard refresh.
- **Modrý pruh pod fotkou byl ale reálná nová chyba** – přímý důsledek zmenšení přesahu karet v minulém kole (margin-top z -7rem na -2/-2,5/-3rem). Tahle nová, menší hodnota nechávala mezi spodkem fotky a vrškem karet mezeru 15–32 px (přeměřeno `getBoundingClientRect` na 1920/768/390px) – na modrém pozadí `.page-hero` (rezerva `padding-bottom:var(--space-16)`) to vypadalo jako samostatný pruh. **Fix:** `margin-top` sjednocen na `-4rem` na všech šířkách – přesně odpovídá `.page-hero padding-bottom` (`var(--space-16)` = 64px), takže karty teď začínají přesně na spodní hraně fotky/tlačítek (mezera ~0 px, na 1920px dokonce jemných -2,5 px = karty se fotky/tlačítek jen sotva dotýkají), bez zásahu DO fotky (to byl problém předminulého kola) i bez viditelné mezery pod ní. Odstraněny i samostatné breakpoint-specifické hodnoty (-2/-2,5/-3rem) – jedna hodnota funguje napříč šířkami, protože rezerva v `.page-hero` je pevná (64px), ne závislá na výšce fotky/textu.
- Ověřeno `getBoundingClientRect` (clearance fotka→karty: 0 px na 1440/1024/768/390px, -2,5 px na 1920px) + screenshoty na 1920/390px + kontrola přetečení na 1920/1440/1024/768/390px (všude bez přetečení).

### Patnácté kolo – hero fotka: celý nezořezaný záběr (maximum možného oddálení)

Uživatelka i po cache-bust (`?v=3`) a doporučení soukromého okna trvala na tom, že fotka je "pořád stejná", a poslala srovnávací screenshoty (požadovaný vzhled × aktuální stav). Rozdíl mezi screenshoty byl minimální/prakticky nerozeznatelný – šlo tedy zjevně o to, že i "vrácený originál" (ořez `(1250,0)-(5472,3648)`) byl podle uživatelky pořád moc přiblížený, ne o to, že by se soubor nenačetl.

- **Použita celá, zcela nezořezaná zdrojová fotka** (`Obrazky/kolaz/close-up-circuit-breakers-wire-control-panel.jpg`, 5472×3648, poměr 3/2) zmenšená na 1800×1200 – ŽÁDNÝ ořez, jen změna rozlišení. Uživatelka explicitně svolila jít až na hranici možného ("ještě více oddálit, pokud to fotka dovoluje") – tohle je doslova ten limit, dál to bez oříznutí panelu nejde (obrázek by se musel upscalovat/rozmazat, nebo přidat obsah mimo fotku, což nejde). Nově viditelná i rozostřená betonová zeď vlevo od rozvaděče (dřív odříznutá) – akceptováno jako cena za maximální možné oddálení, uživatelka na to byla předem upozorněná v odpovědi.
- `.page-hero-media { aspect-ratio }` přepočítáno na `3/2` (přesný poměr nového souboru), `width`/`height` atributy v HTML na `1800×1200`.
- **Cache-busting query string** (`?v=4`, zvýšeno z `?v=3` z předminulého pokusu) – zajišťuje, že i kdyby šlo o cache (nepotvrzeno, ale nevyloučeno), nový soubor se načte jistě.
- Ověřeno: clearance mezi fotkou a kartami "Co nabízíme" pořád 0 px (marže `-4rem` z minulého kola beze změny funguje i s novým, nižším poměrem fotky), bez horizontálního přetečení na 1920/1440/390px, screenshoty potvrzují viditelně širší záběr (beton. zeď + celá šířka panelu vpravo, kde jsou jističe useknuté už ve zdrojové fotce – to už fyzicky víc vidět nejde).

### Šestnácté kolo – fotka natažena AŽ ZA karty (skutečné odstranění modrého pruhu, ne jen doladění mezery)

Uživatelka trvala na tom, že modrý pruh pod fotkou je pořád tam, a přesně specifikovala řešení: fotku roztáhnout dolů, AŽ ZA karty (ne jen minimalizovat mezeru mezi fotkou a kartami, jak řešilo 14. kolo).

- **Restrukturalizace `.page-hero-media` na desktopu (1024px+) – z flex položky na `position:absolute` přes celou výšku `.page-hero`** (stejná technika jako `.hero-media` na homepage): `top:0; left:58%; right:0; bottom:0`, takže fotka teď fyzicky sahá až na úplný spodek sekce (včetně `padding-bottom` rezervy), ne jen po výšku textového sloupce. Karty "Co nabízíme" tak leží PŘÍMO NA fotce (přesah `-4rem` z patnáctého kola teď zabírá do fotky, ne do prázdné modré plochy) – přesně podle zadání.
- **Cestou odhaleny a opraveny DVĚ další chyby, které bránily první verzi tohohle fixu fungovat:**
  1. `.page-hero-media` bylo v HTML vnořené uvnitř `.page-hero-split`, které má `data-reveal` → `[data-reveal].is-visible{transform:translateY(0)}`. I nulový transform ale zakládá containing block pro potomky s `position:absolute` – `.page-hero-media` se tak i po nastavení `position:absolute` přichytávala na `.page-hero-split` (= výška textu, bez rezervy), ne na `.page-hero`. Ověřeno přes `el.offsetParent` (ukazoval `page-hero-split` místo `page-hero`). **Fix:** `.page-hero-media` přesunuta v HTML VEN z `.page-hero-split`, je teď přímý sourozenec uvnitř `.page-hero`, s vlastním `data-reveal` (stejný fade-in efekt, bez vedlejšího containing-block problému).
  2. Po opravě (1) měla `.page-hero-media` sice správný containing block, ale i tak byla široká přes celou obrazovku místo 42 % – `width:100%` ze základního (mobilního) pravidla nebylo v 1024px+ bloku přebité, a explicitní `width` má přednost před dopočtem šířky z `left`+`right`. Ověřeno `getBoundingClientRect` (šířka 1920px místo očekávaných ~806px). **Fix:** `width: auto` přidáno do 1024px+ pravidla.
- **`object-position: right center`** přidáno na `.page-hero-media img` – rozvaděč leží v pravé části zdrojové fotky (nezořezaná verze z 15. kola má vlevo rozostřenou zeď), bez tohohle `object-fit:cover` ořezával symetricky od středu a ve vyšším/užším boxu (fotka teď sahá přes celou výšku sekce) ukazoval hlavně rozostřenou levou část místo jističů.
- Mobil/tablet (<1024px) beze změny – tam už bylo `clearance:0` spolehlivé i předtím (rezerva i přesah jsou tam obě pevné hodnoty nezávislé na výšce textu, na rozdíl od desktopu).
- Ověřeno `getBoundingClientRect` (fotka na desktopu teď sahá přesně po `.page-hero` bottom, karty leží 64px DO fotky = přesně na ní) + screenshoty na 1920/1440/1024/390px + kontrola přetečení všude bez problému.
- **Poučení pro příště:** `[data-reveal]` (`transform: translateY(...)`) na jakémkoli elementu založí containing block pro `position:absolute` potomky, i ve stavu `translateY(0)` (nulový transform pořád je transform, ne `none`). Absolutně pozicovaný prvek, který se má vázat na vzdálenějšího předka (ne na nejbližší wrapper), nesmí být vnořený uvnitř žádného elementu s `data-reveal`/transformem – buď ho přesunout mimo, nebo mu dát vlastní `data-reveal`.

### Sedmnácté kolo – souměrný, větší padding kolem hero textu

Uživatelka potvrdila, že fotka je teď v pořádku, a požádala o větší, souměrný padding nad nadpisem a pod tlačítky v hero textu (aby karty "Co nabízíme" sklouzly ještě kousek níž – přímý důsledek zvětšení `.page-hero-content`, viz 16. kolo, kde `.page-hero` výška = výška textového sloupce).

- `.page-hero-content` základní (mobil/tablet) pravidlo bylo NESOUMĚRNÉ (`padding: var(--space-12) var(--tunnel-inset) var(--space-8)` = 48px nahoře, 32px dole) – sjednoceno na souměrných `var(--space-16)` (64px) nahoře i dole.
- Desktopové (1024px+) pravidlo bylo už souměrné, ale menší (`var(--space-16)` = 64px) – zvětšeno na `var(--space-24)` (96px) nahoře i dole.
- Vyšší textový sloupec → vyšší `.page-hero-split` → vyšší `.page-hero` → karty "Co nabízíme" (fixní přesah `-4rem` od spodku `.page-hero`, viz 16. kolo) se posunuly níž, fotka (absolutně natažená přes celou výšku `.page-hero`) se úměrně prodloužila spolu s ní – žádná zvláštní úprava potřeba, mechanismus z minulého kola funguje automaticky i při jiné výšce obsahu.
- Ověřeno screenshoty + kontrola přetečení na 1920/1440/1024/768/390px – všude bez přetečení, padding viditelně větší a souměrný na obou stranách textu.

**Nevyřízeno:** uživatelka zmínila, že našla vhodnější fotku rozvaděče ("foto: fjhasj") pro hero sekci, ale název/soubor se nepřenesl (nejde o platnou cestu k souboru v projektu). Čeká se na skutečné dodání fotky (příloha nebo platný název souboru), než půjde vyměnit.

### Osmnácté kolo – výměna hero fotky za `rozvadec.jpg` (víc řad jističů, čitelné popisky)

Uživatelka upřesnila cestu k nalezené fotce: `Obrazky/kolaz/rozvadec.jpg` (2527×3075, na výšku, poměr ~0,82) – ukazuje 2–3 celé řady jističů s popisky obvodů, přepínači "I ON" a barevně značenými vodiči, výrazně bohatší záběr než dřívější panoramatická fotka.

- Nahrazen `Obrazky/web/hero-elektro-panel.jpg` (zmenšeno na 1600×1947, beze změny poměru/ořezu – celá fotka). `width`/`height` atributy a alt text v HTML aktualizovány, cache-busting `?v=5`.
- **`.page-hero-media` aspect-ratio (mobil/tablet)** změněn z `3/2` (pro starou panoramatickou fotku) na `4/3` – nová fotka je na výšku, `4/3` ořízne shora/zdola míň než dřívější širší poměr.
- **`object-position`** změněn z `right center` (dřívější fotka měla ostrý motiv vpravo, vlevo rozostřenou zeď) na `center 20%` – nová fotka má zajímavý obsah rovnoměrně přes celou šířku, ale nejnižší řada jističů je už oříznutá ve zdroji, proto ořez shora dolů (preferovat horní řady) místo od středu.
- Ověřeno screenshoty na 1920/1440/1024/768/390px – bez horizontálního přetečení, na všech šířkách viditelné 2+ celé řady jističů s popisky, fotka pořád sahá až za karty "Co nabízíme" (mechanismus z 16. kola funguje beze změny i s novou fotkou/poměrem).

### Devatenácté kolo – ořez fotky posunut výš
`object-position` z `center 20%` na `center 5%` – odkrývá víc vrchní části fotky (vodiče a horní řadu jističů), méně spodku. Ověřeno na 1920/390px bez přetečení.

### Dvacáté kolo – "horní rozvaděč" neexistuje (limit zdrojové fotky), ořez vrácen zpět na kabely uprostřed

Uživatelka chtěla posunout ještě víc nahoru, "aby vylezl i ten horní rozvaděč". Zkoušeno `object-position: center 0%` (matematické maximum – `0%` = žádný ořez shora, obraz už nejde posunout výš). Přímá kontrola vrchního okraje zdrojové fotky (`Obrazky/kolaz/rozvadec.jpg`, crop `(0,0,2527,600)`) potvrdila, že nad vodiči je jen kousek bílé stěny rozvaděče – žádný další (horní) panel na fotce vůbec není, takže ho nejde odkrýt žádnou úpravou ořezu. Sdíleno s uživatelkou přímo.

Uživatelka pak požádala o posun opačným směrem, ať jsou kabely (svazek s páskami) uprostřed fotky. `object-position: center 25%` byla ještě moc nízko (uživatelka nahlásila, že svazek je pořád ve spodní části). Doladěno na `center 12%` – svazek teď leží skutečně uprostřed viditelného výřezu. Ověřeno screenshoty + kontrola přetečení na 1920/1440/1024/768/390px – všude bez přetečení.

### Dvacáté první kolo – uprostřed pořád jistič: kořenová příčina (proměnlivá výška boxu podle šířky) a přeořez fotky

Uživatelka nahlásila, že uprostřed vidí pořád jistič, ne kabely. Diagnostika: `.page-hero-media` box má na desktopu FIXNÍ výšku ~637px (danou výškou textového sloupce, která se u typických laptop šířek 1366–1920 skoro nemění), ale ŠÍŘKU proměnlivou (42 % viewportu) – viditelný podíl fotky (kolik % výšky zdrojové fotky se vejde do boxu) se proto breakpoint od breakpointu výrazně liší (přeměřeno: 65 % viditelno při 1920px, 87 % při 1440px, 100 % při 1366px a užších). Jedna pevná hodnota `object-position` proto nemohla udržet svazek vodičů (ležící jen v horních ~11 % zdrojové fotky, přeměřeno přímým oříznutím vrchní části) opticky uprostřed napříč všemi šířkami zároveň – matematicky to není řešitelné jen posunem, dokud fotka sama obsahuje tolik "navíc" výšky pod svazkem.

**Fix:** fotka přeořezána u zdroje (`Obrazky/kolaz/rozvadec.jpg`, ořez `(0,0,2527,2100)` místo celé výšky 3075 px) – ponechává svazek vodičů + JEDNU celou řadu jističů (kompaktnější, zaostřenější kompozice), odstraňuje druhou/třetí řadu, které předtím jen "natahovaly" fotku a zvětšovaly rozptyl mezi breakpointy. Uloženo jako 1800×1496 (poměr ~1,2 – mnohem blíž typickým poměrům boxu než předchozí poměr ~0,82, tedy menší potřebný ořez). `aspect-ratio` na mobilu/tabletu přepočítán na `6/5`. `object-position` zjednodušen na `center 20%`.

Ověřeno screenshoty na 1920/1440/1366/1024/390px – svazek vodičů je teď viditelný a opticky blízko středu na všech těchto šířkách (ne dokonale matematicky uprostřed úplně všude, ale konzistentně, bez toho, aby na některé šířce vyloženě "vypadl" a nahradil ho osamocený jistič uprostřed). Bez horizontálního přetečení.

**Poučení pro příště:** u `.page-hero-media` (fixní výška boxu, proměnlivá šířka) je ladění `object-position` jen jedním číslem spolehlivé jen tehdy, když zdrojová fotka nemá o moc jiný poměr stran než box – když je rozdíl velký (fotka na výšku, box na šířku), viditelný podíl fotky se mezi úzkými/širokými breakpointy mění tak dramaticky, že žádná jedna hodnota nesedí všude. Řešení je přeořezat/zmenšit zdrojovou fotku blíž k poměru boxu, ne donekonečna ladit procenta.

### Dvacáté druhé kolo – vyzkoušeno `center 50%`, pak fotka vrácena na nezořezaný originál

Na dotaz "co když dám center 50%" ověřeno screenshoty na 1920/1440/1024/390px – vizuálně identické s `center 20%`. Vysvětlení: po přeořezání fotky v předchozím kole (kratší/kompaktnější výřez, poměr ~1,2) se teď na většině šířek zobrazuje skoro celá výška fotky (box ořezává hlavně po stranách, ne nahoře/dole) – svislá pozice `object-position` už tedy nemá skoro žádný viditelný efekt.

Uživatelka pak požádala vrátit fotku na PŮVODNÍ nezořezanou verzi (bez zásahu z 21. kola). Vráceno: `Obrazky/web/hero-elektro-panel.jpg` znovu celá fotka `rozvadec.jpg` beze změny/ořezu (jen zmenšeno na 1600×1947), `width`/`height` v HTML zpět na 1600×1947, cache-busting `?v=7`. `.page-hero-media` aspect-ratio vráceno na `4/3`, `object-position` na `center 20%`. Vědomý kompromis zapsán do kódu komentářem: s nezořezanou fotkou (poměr ~0,82) a proměnlivou výškou boxu podle breakpointu (viz 21. kolo) nebude `object-position` sedět úplně stejně na všech šířkách – uživatelka dala přednost celistvosti fotky před dokonalým centrováním. Ověřeno screenshoty + kontrola přetečení na 1920/1440/1024/768/390px – bez přetečení.

Následně uživatelka postupně doladila `object-position` ručně (40 % → 30 % → 25 %), pokaždé ověřeno bez horizontálního přetečení na 1920/1440/1024/768/390px. Finální hodnota: `center 25%`.

### Dvacáté třetí kolo – chybějící mezera mezi "Potřebujete elektrikáře rychle?" a Elektrorevize

Uživatelka trvala na tom, že padding kolem pásu "Potřebujete elektrikáře rychle?" neodpovídá homepage, přestože `getComputedStyle` `.band` samotného vycházelo identicky (48/64/48px). Skutečná příčina byla jinde: na homepage je za `.band` sekce `.about-section` s vlastním `padding-top:var(--space-16)` (64px), který vytváří viditelnou bílou mezeru před další sekcí. Na podstránce je za `.band` `.revize-section`, jejíž `padding-top` je ale záměrně `0` (oprava "modrého pruhu" z 16./22. kola – `.revize-section` i fotka uvnitř sdílí stejné pozadí, takže padding-top by se tam propsal jako modrý pruh). Vedlejší efekt téhle opravy: zmizela i mezera MEZI pásem pohotovosti a Elektrorevize, sekce na sebe nalepovaly (ověřeno screenshoty vedle sebe – na homepage zřetelná bílá mezera, na podstránce sekce hned navazuje).

**Fix:** přidán `margin-top: var(--space-16)` (64px) na `.revize-section` (s `@media max-width:767px` zmenšením na `var(--space-12)`/48px, stejná škála jako `--section-padding-mobile` jinde na webu). Margin místo paddingu – margin nevykresluje pozadí sekce, takže mezera zůstane bílá (stejně jako na homepage), ne modrá – bez rizika, že se bug s pruhem vrátí (ověřeno screenshoty, žádný modrý pruh se neobjevil).

Ověřeno srovnávacími screenshoty homepage × podstránka (stejný ořez kolem `.band`) na 1440/768/390px – mezera teď vizuálně odpovídá. Bez horizontálního přetečení na 1920/1440/1024/768/390px.

### Dvacáté čtvrté kolo – modré pozadí Elektrorevize zúženo jen na fotku/text, ostatní bílé

Uživatelka chtěla, aby modré pozadí v sekci Elektrorevize bylo jen na úrovni fotky (foto+text řádek), karty "Provádíme revize" a "Jak elektrorevize probíhá" pod tím byly na bílém a modrá se vracela až v "Otázky a odpovědi".

- `.revize-section { background: var(--color-bg-blue) }` odstraněno – sekce je teď bez vlastního pozadí (bílá, dědí z běžného pozadí stránky). Modré zůstává jen na `.about-split` (to má vlastní `background: var(--color-bg-blue)` v základním pravidle nezávisle na `.revize-section` – beze změny), tedy přesně na úrovni fotky a textu "Elektrorevize".
- `padding-top: 0` na `.revize-section` ponecháno beze změny (fotka dál sedí na vrchu sekce, viz 16./23. kolo) – i po odstranění modrého pozadí sekce je to pořád potřeba, jinak by nahoře vznikla bílá mezera nad fotkou.
- Karty "Provádíme revize" (skleněné, poloprůhledné bílé) zůstávají čitelné i na bílém pozadí – horní část každé karty přesahuje přes spodní hranu fotky (stín + jemný rámeček dost odliší od pozadí), stejný princip jako u "Co nabízíme" karet na bílé ploše výše na stránce.
- Zkontrolovány paddingy `.section--blue` (Otázky a odpovědi) a `.section.showcase-section` (Ukázky naší práce) – beze změny, obě už odpovídaly systémovému vzoru webu (64/64 desktop, 48/64 mobil u FAQ; 64/0 u showcase, shodné s homepage) – vizuálně (screenshoty) teď se změněným bílým pozadím Elektrorevize navazují čistě, žádná úprava nebyla potřeba.
- Ověřeno screenshoty na 1440/768/390px (`.revize-section` samostatně + přechod do FAQ/showcase) a kontrola přetečení na 1920/1440/1024/768/390px – bez přetečení, modrá plocha teď přesně kopíruje jen řádek s fotkou/textem.

### Dvacáté páté kolo – padding CTA sekce zmenšen na 64px

Uživatelka se zeptala, proč `.page-cta` ("Máte zájem o elektro služby?") má jiný padding než FAQ/Ukázky – vysvětleno: 96px na desktopu, protože kopíroval homepage `.contact-section` (větší, "závěrečný CTA" padding), zatímco FAQ/Ukázky mají běžných 64px. Uživatelka chtěla radši sjednotit na 64px. Základní pravidlo `.page-cta` přepnuto z `var(--space-24)` (96px) na `var(--space-16)` (64px) – tabletové pravidlo (768–1023px) bylo už 64px, teď redundantní, ale neškodí. Mobilní `--section-padding-mobile` (48px) beze změny. Ověřeno `getComputedStyle` na 1920/1440/1024/768/390px (64/64/64/64/48) + kontrola přetečení – bez přetečení.

### Dvacáté šesté kolo – padding CTA na 80px, sjednocení mezer u nadpisů, kolotoč hero min-height (beze změny), nové ikony "Co nabízíme", ikony kroků, oprava gramatiky a bílé ikony u "Provádíme revize"

Delší sada drobných doladění napříč celou stránkou `elektro-sluzby.html`.

- **`.page-cta` padding znovu upraven, tentokrát na ~80px.** Po 25. kole (64px) chtěla uživatelka hodnotu "kolem 80" – mezikrok mezi FAQ/Ukázky (64px) a homepage `.contact-section` (96px). `padding-block` na desktopu nastaven na `5rem` (80px, natvrdo, ne `--space` proměnná – na škále není token přesně na 80px). Tablet/mobil beze změny (64px/48px).
- **Sjednocení mezery nadpis→obsah u "Ukázky naší práce" a "Otázky a odpovědi".** `.showcase-section .section-head` mělo vlastní `margin-bottom: var(--space-8)` (32px), zatímco FAQ dědilo výchozí `.section-head { margin-bottom: var(--space-12) }` (48px). Uživatelka chtěla obě stejné, větší z dvojice → scoped pravidlo u `.showcase-section` smazáno, teď dědí stejných 48px jako FAQ.
- **Min-height `.page-hero` – kolo tam a zpátky, beze změny.** Uživatelka nejdřív chtěla hero sekci vyšší (přidán `min-height` base + `@media(min-width:1024px)` override), pak "něco mezi" (zmenšeno), nakonec požádala vrátit přesně původní hodnotu (637px) – obě pravidla nakonec úplně smazána. Ověřeno Playwright měřením výšky (`636.9375px`/`711.328125px`), přesně odpovídá stavu před tímhle kolem – čistý revert, žádný čistý efekt na výšku.
- **"Co nabízíme" – 6 SVG ikon nahrazeno novými PNG ikonami uživatelky, styl "jako karty služeb na homepage" (bez kolečka, glow efekt), zmenšeně.** Nové ikony (`Obrazky/Ikony/*.png`) jsou barevné (navy+oranžová), ne jednobarevné jako dřívější SVG – proto nešlo použít výchozí `.feature-item-icon` (navrženo pro bílé ikony v oranžovém kolečku). Přidán scoped `.offer-grid .feature-item-icon` (bez pozadí/kolečka/stínu, `width/height:3rem`) + `.offer-grid .feature-item-icon img` s vícevrstvým `drop-shadow` glow filtrem identickým s `.service-card-icon img` (velké ikony pod hero na homepage), jen v menším měřítku. Finální PNG uloženy jako `Obrazky/web/icon-nove-elektroinstalace.png`, `icon-rekonstrukce-elektroinstalaci.png`, `icon-osvetleni.png`, `icon-rozvadece-a-jisteni.png`, `icon-chytra-domacnost.png`, `icon-servis-a-opravy.png`.
  - **Chyba a oprava — nekonzistentní velikost ikon.** První ořez (Python/Pillow) používal `Image.getbbox()`, které počítá do bounding boxu i téměř neviditelný "prach" (alfa 1–9, stovky až tisíce pixelů daleko za skutečným motivem) – množství prachu se lišilo obrázek od obrázku, takže i při stejné relativní logice paddingu vyšly ikony viditelně různě velké (nejvíc patrné u "domečků"). Uživatelka to sama odhalila a požádala předělat z čerstvých originálů. **Fix:** ořez přepočítán přes numpy s prahem `alpha > 20` (ignoruje prach) + fixní 15px okraj (zachová skutečné antialiasing hrany) + 8% relativní padding do čtverce – sjednocený poměr "fill fraction" (`max(bw,bh)/side`) 0,926 napříč všemi 6 ikonami, ověřeno numericky i vizuálně (uživatelka potvrdila: "vypadá to dobře").
- **"Jak elektrorevize probíhá" – čísla kroků nahrazena novými ikonami (blesk s číslicí 1/2/3), stejný glow efekt jako u karet "Co nabízíme".** Uživatelka nahrála jednu sdílenou ikonu (`Obrazky/Ikony/ikony cisel.png`, čísla 1/2/3 s bleskem) – rozdělena na 3 samostatné soubory (`icon-krok-1.png` 357×544, `icon-krok-2.png` 470×555, `icon-krok-3.png` 475×568), stejný glow `drop-shadow` filtr jako u offer-grid ikon výše aplikován na `.step-number img`.
  - Staré spojovací šipky/chevrony mezi kroky (zavedené ve 12. kole) na žádost uživatelky smazány (`.step-item:not(:last-child)::after`, base i 768px+ varianta odstraněny beze náhrady).
  - **Chyba a oprava — zarovnání čísel s textem.** Číslice byly nejdřív oříznuté do vynuceného ČTVERCE (stejná technika jako u kruhových ikon výše) – užší "1" tak dostala mnohem víc prázdného vodorovného paddingu než širší "2"/"3", takže vizuálně nesedělo zarovnání zleva s nadpisem/textem pod ikonou. **Fix:** ořez přepracován bez vynuceného čtverce – zachován přirozený poměr stran každé číslice zvlášť (10% padding na každé ose nezávisle), `.step-number` v CSS přepnut z centrovaného pevného boxu na `height:3.25rem; width:auto` s výchozím (levým) zarovnáním flexu – ikony teď sedí na společné výškové ose a jejich levý okraj se shoduje s textem pod nimi. Ikony nemají žádný bílý rámeček/okraj (průhledné PNG s malým průhledným okrajem) – ověřeno na dotaz uživatelky.
- **Gramatická oprava 4 nadpisů karet "Provádíme revize"** (genitiv → nominativ): "Rodinné domy a bytové objekty", "Komerční a průmyslové objekty", "Hromosvody", "Elektrospotřebiče a nářadí".
- **Ikony týchž 4 karet vyměněny za novou sadu bílých PNG ikon uživatelky**, styl "jako ikony v kartách O nás na homepage" (bílá ikona v oranžovém gradientovém kolečku) – žádná nová CSS nebyla potřeba, karty už používaly výchozí `.feature-item-icon` recepturu (2,3rem ikona v 3,5rem kolečku), stačilo vyměnit `<svg><use></svg>` za `<img>`. Zdrojové soubory `Obrazky/Ikony/rodinne-domy.png`, `komercni-prumyslove-objekty.png`, `hromosvody.png`, `elektrospotrebice-naradi.png` ořezány stejnou čtvercovou pipeline (`alpha>20`, 15px okraj, 8% padding, fill=0,926) jako "Co nabízíme" ikony výše → `icon-rodinne-domy.png`, `icon-komercni-prumyslove-objekty.png`, `icon-hromosvody.png`, `icon-elektrospotrebice-naradi.png`.
- **Galerie "Ukázky naší práce" – kompletně přeobsazena novými fotkami uživatelky** (montáž rozvaděče, elektroměry, venkovní rozvaděč, zapojení rozvaděče, veřejné osvětlení, příprava rozvaděčů), včetně dvou kol přeskládání pořadí na žádost uživatelky. Nové soubory `Obrazky/web/realizace-elektro-montaz-rozvadece.jpg`, `-rozvadec-elektromery.jpg`, `-venkovni-rozvadec.jpg`, `-zapojeni-rozvadece.jpg`, `-verejne-osvetleni.jpg`, `-priprava-rozvadecu.jpg`. Staré `realizace-elektro.jpg`/`-2.jpg` ponechány beze změny (dál používá `index.html`).
- Všechny body ověřeny Playwright screenshoty (různé šířky) i vizuální kontrolou uživatelkou přímo v konverzaci.

**Otevřené:** žádné explicitně nevyřízené požadavky v tomto kole.
